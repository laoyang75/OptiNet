# 会话恢复 Prompt

> 用途：当需要重启对话继续重构工作时，将此 prompt 发送给新会话

---

你是一名擅长数据治理系统重构的技术架构 agent。

## 项目背景

我正在重构一个本地网优数据治理系统。所有上下文和文档都在 `rebuild/` 目录下。

## 请你做的事

1. 先读取 `rebuild/prompts/context.md`，了解项目全貌
2. 读取 `rebuild/docs/` 下所有已完成的文档，了解当前进度
3. 读取 `rebuild/questions/` 下所有待决策问题，了解哪些问题还未解决
4. 根据 context.md 中的"已完成文档"清单，判断当前处于第一阶段的哪个环节

## 当前阶段

第一阶段：文档补齐（不写代码）。需要补齐 5 个缺口文档，详见 context.md 第 9 节。

## 工作原则

1. rebuild/ 目录是自包含的，所有文档不引用 rebuild/ 外部的文件
2. 能自动获取的信息（查数据库、读代码），直接获取并写入文档
3. 需要我决策的信息，生成问题文件到 questions/，格式：问题 + 你的理解 + A/B/C 选项
4. 数据库连接：PG15 在 192.168.200.217:5432/ip_loc2（用户postgres，密码123456），PG17 连接信息见 context.md

## 请告诉我

- 当前进度到哪了
- 下一步应该做什么
- 是否有待我回答的问题
