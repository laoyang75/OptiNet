# rebuild2 任务清单

> 每个 Layer 包含三件事：SQL（建表/写数据）、API（查询接口）、UI（可视化验证）
> 完成标准：**人通过 UI 确认数据正确，才进入下一层**

---

## Layer 0：字段审计

| # | 任务 | 产出 | 状态 |
|---|------|------|------|
| L0-1 | 搭建基础框架（FastAPI + 前端壳 + 样式） | 可运行的空工作台 | |
| L0-2 | 字段审计 API：读 legacy 两表元数据 + 采样 | `/api/v1/audit/fields`, `/api/v1/audit/sample` | |
| L0-3 | 字段审计页 UI：字段列表 + 分布 + 标注 | P0 字段审计页 | |
| L0-4 | 人工完成 42 个字段标注 | `docs/01_字段契约_Layer0.md` 填充完成 | |

---

## Layer 1：ODS 标准化

| # | 任务 | 产出 | 状态 |
|---|------|------|------|
| L1-1 | 基于字段契约编写 ODS DDL | `sql/01_ods.sql` → `ods.cell_records` | |
| L1-2 | 编写 ETL SQL：两表合一 + 字段标准化 | `sql/02_ods_etl.sql` | |
| L1-3 | 数据概览 API + 页面 | P1 数据概览页：行数、质量热力图、分布 | |
| L1-4 | 人工验证 ODS 数据正确性 | 确认合并后行数 ≈ 2.51 亿 | |

---

## Layer 2：可信库

| # | 任务 | 产出 | 状态 |
|---|------|------|------|
| L2-1 | 可信 LAC DDL + 筛选 SQL | `dim.lac_trusted` | |
| L2-2 | Cell 统计 DDL + 聚合 SQL | `dim.cell_stats` | |
| L2-3 | 合规过滤 DDL + 回写 SQL | `dwd.fact_filtered` | |
| L2-4 | 可信库 API + 页面 | P2 可信库页：漏斗、筛选统计 | |
| L2-5 | 人工验证可信边界 | LAC ~881, Cell ~50 万 | |

---

## Layer 3：BS + GPS + 信号

| # | 任务 | 产出 | 状态 |
|---|------|------|------|
| L3-1 | BS 主库 DDL + 聚合 SQL | `dim.bs_trusted` | |
| L3-2 | GPS 修正 DDL + 修正 SQL | `dwd.fact_gps_corrected` | |
| L3-3 | 信号补齐 DDL + 补齐 SQL | `dwd.fact_signal_filled` | |
| L3-4 | BS 页 + GPS 修正页 + 信号页 | P3 BS/GPS/信号页 | |
| L3-5 | 人工验证修正质量 | GPS 覆盖 > 80%, 信号补齐 > 70% | |

---

## Layer 4：完整回归

| # | 任务 | 产出 | 状态 |
|---|------|------|------|
| L4-1 | 最终明细 DDL + 回归 SQL | `dwd.fact_final` | |
| L4-2 | 回归对比 API + 页面 | P4 回归对比页 | |
| L4-3 | 人工验证完整性 | 最终明细 ~3000 万行 | |

---

## Layer 5：画像基线

| # | 任务 | 产出 | 状态 |
|---|------|------|------|
| L5-1 | 三级画像 DDL + 聚合 SQL | `dws.profile_lac/bs/cell` | |
| L5-2 | 画像 API + 页面 | P5 画像页 | |
| L5-3 | 人工验证画像完整性 | LAC ~878, BS ~16 万, Cell ~49 万 | |
