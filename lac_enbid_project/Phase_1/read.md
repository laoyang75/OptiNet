
> 已合并为单一交付文档：`Phase_1_Engineering_Handoff.md`（含流程图/拍板口径/数据契约/验收清单）。本文件保留决策原稿与补充说明。

以下是对你的问题回答，但有些我必须说清楚，你的这份文档对于人类优化，是第一位的。

我们的研究过程，并不是工程化，所以我们呈现的完成流程的合理逻辑，为了方便对齐，他需要有清晰逻辑，流程图。原因说明。

但并不需要太复杂的代码优化，因为这个最后不是在当前的数据库中跑，这个的目的是让工程团队将数据清洗补数流程化动态话。最后可以形成有效的 lac enbid cell 的汇总表，以及独立的数据补齐的明细表，并钱有清晰的数据关系，在这个基础上会在做逻辑解决问题。

所以让工程团队，快速理解看懂，比代码更重要。我们目前的内容是研究性的，所以很多会分很多步骤，有些事为了谨慎，有些事为了逻辑清楚。但我们现在已经完成了工作，所以需要通过宏观来做整体梳理。

## 4. 需要你拍板的讨论问题（用于生成“合理流程”）

1) 严重碰撞桶：是否永久采用“回填但强标注”的策略？下游是否必须默认过滤？  

在本期中采用标注，并回填，因为我需要了解规模以及随数据量变化是否规模会变得更大。然后在通过类似发现活动cell的方式逐渐，解决，所以目前在数据不充分的情况下，我无法给出准确的逻辑。不需要默认过滤，确保可见。

2) Layer_5 是否必须覆盖 `lac_dec_final IS NULL` 的对象？若需要，用哪种规范表达（`LAC=-1` / 单独表 / 单独标签）？  

不太理解，对于 lac为null -1 1 0 以及lac小于 十六进制的 0101的全部是无效lac，这个应该在 Layer_1 就明确，他不跟本不应该进入 Layer_5 如果是cell_id回补时引入的，他也需要更新为有效的，这些都是无效值

3) 城市/非城市阈值：下一阶段是否引入 lac 城市分类表，还是先维持城市阈值并仅做标注？  

未来的单独讨论，目前是计划引入但策略可能不是简单的根据城市，而是根据实际密度跑下，比如发现大量cell的gps超限是有特征，可能会吧这些lac定义为非城市，或长程的lac，目前不确定你可以他写入下一阶段的Lac画像研究。

4) bs_id 异常：是否进入 Layer_5 画像的显式标签字段（而不是仅保留 Step44/36 标注表）？  

需要写入，数据必须完全可见在任何阶段都能看到问题

5) 信号补齐评估：是否把“关键字段集合”先固定下来（RSRP/RSRQ/SINR/DBM），以便 Phase_2 做加权公式时有稳定口径？

这个做标注，但目前还没有数据，所以处于待更新状态。

---

## 5. 对“数据可穿透性”的最终建议（供后续流程固化）

建议把“可穿透性”作为工程硬要求写入项目流程：

- 每层主表必须能回答：输入是谁、输出是谁、关键字段如何变化、质量标记如何变化、收益如何量化。  
- 每层至少保留三类字段：
  1) 主键与分桶键（对象可定位）  
  2) before/after（例如 `lon_before_fix → lon_final`，`sig_* → sig_*_final`）  
  3) 来源/策略/原因（例如 `gps_source/gps_fix_strategy/collision_reason/signal_fill_source`）

如果你认可这套评估结论，我建议下一步把“讨论问题的答案”固化为：  
项目级流程图 + 统一字段契约 + 统一验收面板（作为 Phase_1 的最终交付规格）。

可以没问题，字段中文化是必须的选项，因为ai会创建不同的字段，在ai创造的时候看上去合理，但每个阶段都不一样，导致夸Layer的对齐对人类及其困难，我们使用数据库都支持中文字段名称，所以需要在讨论中即使需要用到英文，也必须括号备注中文，确保一致性。这需要作为检查项存在。
