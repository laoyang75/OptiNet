# 冲突与裁决矩阵

用途：记录 Codex / Claude / Gemini 三条线在统一阶段出现的冲突点与裁决结果。

| 编号 | 冲突主题 | Codex 结论 | Claude 结论 | Gemini 结论 | 主控裁决 | 依据 | 是否需人工确认 |
|---|---|---|---|---|---|---|---|
| C-001 | governance 的来源表达 | 倾向把继承真相作为正式可见能力 | 初稿曾写 `real(inherited)` | 初稿曾写 `real(inherited)` | `data_origin` 仍只保留 `real / synthetic / fallback`；治理页若展示可追溯的 rebuild2 继承真相，标记 `real`，并返回 `origin_reason=inherited_from_rebuild2`；若只是样例或手填占位，则必须标记 `fallback` | `data_origin` 合同必须保持三值稳定，但继承真相不能被错误降级成占位 | 否 |
| C-002 | compare / governance 在 IA 中的位置 | 作为支撑验证与治理说明 | 作为支撑模块，不是首页主流程 | 作为支撑治理层，不是主流程层 | 统一归入支撑层，不能抢占首页主链路 | UI_v2 与 rebuild3 04 系列纠偏结论一致 | 否 |
| C-003 | snapshot 的正式主语 | 以真实 batch 内快照为主 | 明确是 `batch + snapshot_timepoint` | 明确是 `batch + snapshot_timepoint` | 正式主语固定为 `batch + snapshot_timepoint`；scenario 仅能作为 synthetic 评估上下文 | rebuild3 偏航问题的核心就是 scenario 抢占主语 | 否 |
