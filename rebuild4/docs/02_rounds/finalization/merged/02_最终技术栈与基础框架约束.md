# 最终技术栈与基础框架约束

状态：finalization merged 冻结稿  
更新时间：2026-04-06  
输入范围：前三轮 merged、全部 decisions、`rebuild4/docs/01_inputs/*`、`PG17`（2026-04-06 复核）。

---

## 1. 冻结结论

本轮最终技术栈与基础框架约束已经收口到唯一方案：

- 数据库事实与正式存储统一落在 PostgreSQL 17。
- 后端统一采用 `Python 3.11+ + FastAPI` 提供 RESTful JSON API，并对统一 envelope 负责。
- 前端统一采用全新 `Vue 3 + TypeScript + Vite` 项目，不从 rebuild3 前端整体 fork。
- 页面验收统一以 Playwright 为正式入口。
- launcher 被明确冻结为独立运维入口，保留接口与实时推送约束，但不进入 G0-G7 成功标准。

---

## 2. 最终技术栈冻结

### 2.1 分层冻结表

| 层级 | 冻结选择 | 最终约束 |
|---|---|---|
| 数据库 | PostgreSQL 17 | 主计算、主存储、主校验环境；所有计数与比例以 PG17 为准 |
| 正式 schema | `rebuild4` + `rebuild4_meta` | rebuild4 业务主表与控制 / 元数据层必须分层 |
| 只读参考 schema | `rebuild2`、`rebuild2_meta`、`rebuild3`、`rebuild3_meta` | 只读参考，不得 current 化或反写 |
| 后端 | `Python 3.11+ + FastAPI` | run / batch 编排、读模型 API、Gate 执行输出统一由后端负责 |
| 查询风格 | SQL-first | 领域真相、聚合、校验、损耗统计以 SQL / 参数化查询为主，不以 ORM 领域模型为主语 |
| 前端 | 全新 `Vue 3 + TypeScript + Vite` 项目 | 承接 `D-002`，项目骨架、路由、状态、请求层全部重建 |
| 前端共享状态 | Pinia | 只承接 UI 共享状态，不承接来源语义判断 |
| 页面验收 | Playwright | 最终正式页面验收入口；本轮不重跑页面，只冻结其地位 |
| 数据库事实复核 | `PG17 execute_sql` | 计数、比例、差集、缺口统一走数据库查询 |
| 结构复核 | `PG17 search_objects` | schema / table / column / index 等结构确认唯一入口 |

### 2.2 明确禁止的技术方向

以下方向未被输入、merged 与裁决授权，明确不进入本轮范围：

1. Kafka / Redis / ClickHouse / Spark / 云调度系统等新增基础设施。
2. 以 rebuild3 的路由、Pinia 结构、axios 拦截层或 fallback/synthetic 判断逻辑为起点继续改造。
3. 以 `rebuild3*` 或 `scenario_replay` 结果顶替 rebuild4 正式主链 real 数据。
4. 用 shell 输出、页面文案或人工台账替代数据库事实。
5. 将 compare 结果、legacy 字段或 launcher 状态冒充主链业务真相。

---

## 3. API 与基础框架合同

### 3.1 统一 API envelope 冻结

所有正式 API 顶层都必须固定包含以下字段：

- `data_origin`
- `origin_detail`
- `subject_scope`
- `subject_note`
- `context.*`
- `data`

固定约束如下：

1. `data_origin` 只允许 `real / synthetic / fallback` 三值。
2. `subject_scope` 只用于标记响应主语，固定在 `run / batch / object / baseline / governance / compare / initialization` 范围内。
3. 主流程 API 只允许返回 `real` 或诚实空状态；`synthetic` / `fallback` 只允许出现在 compare 降级语义中。
4. 前端必须真实消费 `data_origin` 并影响展示；来源语义不能由前端自行发明。

### 3.2 governance 与 compare 的接口边界

| 模块 | 冻结约束 |
|---|---|
| governance | 必须采用多 endpoint lazy-load；不可回退为巨型单接口 |
| ODS | 定义层与执行层必须分离为 `ods_rules` 与 `ods_executions` 两组接口 |
| parse / compliance | 作为 governance 正式能力，必须有独立接口，且正式数据来自 `rebuild4_meta` |
| trusted loss | 只读取预聚合结果，不走页面实时大表联查 |
| compare | 仅辅助验证；允许 `real / synthetic / fallback`，其中 `fallback` 只允许空 envelope |

---

## 4. 前端框架与复用边界（`D-002` 回写版）

### 4.1 允许复用的范围

仅允许在逐组件审计通过后复制以下类型的纯展示组件：

- badge / tag / indicator 类纯渲染组件
- 不含取数逻辑、来源语义判断与 fallback 逻辑的展示卡片
- 与 `UI_v2` 一致的基础视觉组件

### 4.2 禁止复用的范围

以下内容一律禁止直接复制或整体继承：

- rebuild3 路由结构
- rebuild3 Pinia store 结构
- rebuild3 axios 请求层 / 拦截层
- 任意包含 `fallback` / `synthetic` / 主链语义判断的逻辑代码
- 旧前端项目骨架、隐式页面主语和旧状态机

### 4.3 前端正式约束

1. 主流程无 real 数据时必须显示诚实空状态，不能拼 fake 卡片。
2. legacy 信息只允许通过 `legacy_ref` 进入嵌套参考区，不进入主筛选与主状态模型。
3. compare 页必须显式显示“辅助验证”身份，`synthetic` / `fallback` 必须出现 banner。
4. governance 页必须真实消费 ODS 定义 / 执行分离、trusted loss 与规则 seed 的正式接口。

---

## 5. launcher 的最终落点

launcher 不能因为“不计入 Gate”而在最终文档中消失。其基础框架约束冻结如下：

| 主题 | 冻结结论 |
|---|---|
| 身份 | 独立运维入口，不属于数据主链页面 |
| 启动方式 | 独立 Python 脚本启动，不依赖主系统前后端 |
| WebSocket | `/ws/launcher/status`、`/ws/launcher/logs/:service` |
| REST API | `/api/launcher/services*` 系列仅负责服务控制、状态与日志 |
| 与数据合同关系 | 不进入统一数据来源合同，不承接主链业务主语 |
| 与 Gate 关系 | 不计入 G0-G7 成功标准，不阻断正式数据链冻结 |
| 在最终文档中的落点 | `01_最终技术栈与基础框架约束.md` 说明其框架位置；`05_本轮范围与降级说明.md` 说明其不计正式完成 |

---

## 6. 必须带入 `03_final/` 的基础框架约束

1. `contract_version` 必须锚定六文件冻结包，而不是两文件 hash。
2. 前端新项目、统一 envelope、ODS 分离、launcher 独立入口都必须写成最终正式约束，不再停留在 merged 口头结论。
3. 任何正式 API、任务书步骤或校验清单都不得再引用旧 `03_final/01_rebuild4_*` / `02_rebuild4_*` 命名。
4. trusted 损耗指标在正式文档中统一使用 `filtered_with_lon_lat` 命名，不再使用“有 GPS”的模糊表述。

---

## 7. 本轮结论

1. 技术栈已经从“方向冻结”升级为“正式执行约束”。
2. launcher 的边界已经显式落位，不再是隐式排除项。
3. 前端、后端、数据库、验收工具与接口约束都已具备生成 `03_final/` 正式文档的条件。
