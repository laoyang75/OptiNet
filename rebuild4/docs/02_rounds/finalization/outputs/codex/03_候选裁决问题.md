# 候选裁决问题

状态：finalization / codex 复核结论  
更新时间：2026-04-06

---

## 1. 结论

**本轮不建议新增人类裁决问题。**

理由：

1. 方向级分歧已被 round1 `D-001 ~ D-003` 关闭，且已在 round2 / round3 merged 中回写。
2. round2 与 round3 的“无新增裁决”结论，在 finalization 复核后仍成立。
3. 当前发现的问题都属于 finalization 可直接修正的收口项，而不是需要人类在 A / B / C 之间重新拍板的方向冲突。

---

## 2. 不上抛、直接回写的事项

| 编号 | 事项 | 为什么不上抛 | 直接处理方式 |
|---|---|---|---|
| F-CLOSE-001 | `03_final` 任务书 / 校验清单文件名仍沿用旧命名 | finalization prompt 已给出唯一新命名；这是回写修正，不是方向分歧 | 把所有 `03_final/01_rebuild4_最终执行任务书.md` / `02_rebuild4_最终校验清单.md` 改写为 `03_最终执行任务书.md` / `04_最终校验清单.md` |
| F-CLOSE-002 | `contract_version` 仍按“两份文档 hash”叙述 | finalization 已冻结六文件包；这是 manifest 口径修正，不是路线选择 | 将 `contract_version` 对应到 `03_final/00_最终冻结基线.md` 与 `00_05` 组合 hash |
| F-CLOSE-003 | trusted 损耗里的“有 GPS”口径含糊 | PG17 已能证明两个不同口径对应两个不同数字；应做术语修正 | 正式文件统一改成“经纬同时非空 = 11,350,552” |
| F-CLOSE-004 | launcher 在 merged 中只有“排除出 Gate”表述 | 已有 UI_v2 与 launcher 文档足以确定其落点；不需重新裁决 | 在 final 技术栈与范围说明中明确其为独立运维入口、非 G0-G7 成功标准 |
| F-CLOSE-005 | 数据生成/回灌策略仍散在多个 WP / Gate 中 | 是 finalization 文档拆分问题，不是方向冲突 | 汇总到 `03_final/02_数据生成与回灌策略.md` |

---

## 3. 仅在以下异常出现时，才建议新增 finalization 裁决

### F-EX-001：`parse_rule` / `compliance_rule` 无法形成非空正式 seed

触发条件：已经遍历文档、SQL、Python 资料，仍无法在 `rebuild4_meta` 生成非空、可审计 seed。  
届时才需要上抛：是否接受临时占位 seed，或调整 G1 通过标准。

### F-EX-002：客观上无法形成满足 `G3` 的 real rolling 输入窗口

触发条件：无法取得 `completed batch > 1` 的 real rolling 数据窗口。  
届时才需要上抛：是否允许以人工切片方案作为过渡，或接受 G3 暂缓。

### F-EX-003：同一页面在已冻结矩阵下仍出现不可消解的一级主语冲突

触发条件：页面无法在 `run / batch / object / baseline / governance / compare / initialization` 中获得唯一主语。  
届时才需要上抛：修矩阵还是拆页面。

---

## 4. 当前建议

直接进入 finalization merged：

1. 合并 `01_最终冻结基线草案.md`
2. 合并 `02_最终范围技术栈与数据策略草案.md`
3. 保持 finalization `decisions/01_待裁决问题清单.md` 为空清单

当前阶段最需要的是**把收口项写回最终冻结文档**，不是再开一次方向性裁决。
