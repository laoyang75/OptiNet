# 候选裁决问题

状态：finalization Claude 独立输出  
更新时间：2026-04-06  
输入范围：`round1_plan/merged/*`、`round2_detail/merged/*`、`round3_execution/merged/*`、`*/decisions/*`、`01_inputs/*`、`PG17 MCP execute_sql`（2026-04-06）。

---

## 0. 总判断

**本轮无需新增人类裁决问题。**

经逐项检查前三轮 merged 结果、全部裁决（D-001 / D-002 / D-003）、round2 / round3 的候选裁决问题处理记录，以及 2026-04-06 PG17 事实复核，所有方向级、结构级、合同级和 Gate 级的分歧已在 round1-round3 中完全收口。

---

## 1. 检查过程

### 1.1 是否存在三轮之间的逻辑冲突

逐维度检查结果：

| 维度 | round1 结论 | round2 结论 | round3 结论 | 冲突 |
|---|---|---|---|---|
| 数据起点 | rebuild4 自产 | D-001 A：从 l0 重跑 | WP3 冻结 | 无 |
| 来源合同 | real/synthetic/fallback 必须合同化 | API 顶层 envelope | 矩阵逐页冻结 | 无 |
| run_type | — | 三值冻结 | 沿用 | 无 |
| batch_snapshot | — | 4 stage + 11 metric | Gate 校验 | 无 |
| baseline | 单版本允许，禁止伪装 | 按版本追加 | G6 诚实校验 | 无 |
| governance | 纳入正式 | 9 组能力 | G5 + WP7 | 无 |
| compare | 降级 | 语义区分 | G7 | 无 |
| 前端 | — | D-002 C | WP6 Vue3+TS+Vite | 无 |
| ODS | 定义/执行拆层 | D-003 C：两端点 | governance 矩阵行 | 无 |
| parse/compliance | 不能只靠 Markdown | 落 rebuild4_meta | WP2 seed + G1 | 无 |
| legacy 字段 | — | 降级为 legacy_ref | WP6/WP7 约束 | 无 |
| trusted 损耗 | 进入正式范围 | 预聚合读模型 | WP2/WP3 | 无 |

**结论：12 个关键维度无跨轮冲突。**

### 1.2 是否存在已裁决但未回写的条目

| 裁决 | 回写到 round2 merged | 回写到 round3 merged | 回写到校验清单 | 遗漏 |
|---|---|---|---|---|
| D-001 | §1.1.2 | WP3 | C-G2-01~07 | 无 |
| D-002 | — | §6.7 + WP6 | P00~P11 | 无 |
| D-003 | §6.1 第 7/8 项 | 矩阵 governance 行 | C-G5-02 | 无 |

**结论：三项裁决回写完整，无遗漏。**

### 1.3 是否存在 PG17 事实与文档口径的偏差

| 事实项 | PG17 2026-04-06 | 文档声称 | 偏差 |
|---|---|---|---|
| field_audit | 27 (k=17/p=3/d=7) | 27 (k=17/p=3/d=7) | 无 |
| target_field | 55 | 55 | 无 |
| ODS active / exec | 26 / 24 | 26 / 24 | 无 |
| ODS 差集 | 2 条 | 2 条 | 无 |
| parse_rule / compliance_rule | 0 / 0 | 0 / 0 | 无 |
| rebuild4 schema | 不存在 | 不存在 | 无 |
| trusted filter pct | 31.27% | 31.27% | 无 |
| l0_gps / l0_lac | 38,433,729 / 43,771,306 | 38,433,729 / 43,771,306 | 无 |
| 四分流守恒 | 43,771,306 = 43,771,306 | 守恒 | 无 |
| baseline 实体版本 | 各 1 (BASELINE-FULL-V1) | 各 1 | 无 |
| asset_* 四表 | 全部 0 | 全部 0 | 无 |
| compare_result | 0 | 0 | 无 |

**结论：PG17 事实与文档口径完全一致，无偏差。**

### 1.4 是否存在最终口径的多解释空间

逐一检查 round3 任务书中的关键术语：

| 术语 | 是否存在歧义 | 说明 |
|---|---|---|
| `data_origin = fallback` | 否 | 仅用于 compare 空响应降级，data=null |
| `subject_scope` | 否 | 标记 API 响应业务主语，不暗示完整性 |
| `is_current = true` | 否 | 三张表各自独立唯一约束 |
| `诚实空状态` vs `fallback` | 否 | 主流程走空状态，compare 走 fallback |
| `completed batch > 1` | 否 | rolling run 内的 completed batch 数量 |
| `logical batch` | 否 | full_init 对外合同层面的单批次 |
| `legacy_ref` | 否 | API 嵌套参考字段，不进入主状态 |

**结论：所有关键术语已收口到唯一解释。**

---

## 2. 不上抛的候选问题

以下潜在问题曾被考虑，但已被确认不需要上抛：

| 候选 | 不上抛原因 |
|---|---|
| Phase 间并行优化（如 G1 完成后 API 开发与 init 并行） | 实施细节；round3 已冻结严格串行，但 Phase 3 内部 G4/G5/G6 可并行 |
| parse/compliance seed 的具体提取路径 | 实施细节；WP2 已定方向，异常处理在 round3 §8 异常类型 1 |
| rolling 窗口切分粒度 | 实施细节；round3 §8 已明确只要满足 G3 即可 |
| profile_* 实现为表还是 materialized view | round2 §3.3 已允许两种形式，API 合同固定即可 |
| governance 各 endpoint 的分页策略 | 实施细节，不影响合同和 Gate |
| 前端组件审计的灰区判定 | D-002 已给出标准：不包含数据获取逻辑 + 不包含 data_origin 判断 + 样式符合 UI_v2 |

---

## 3. 仅在以下异常情况下才新增裁决

复用 round3 §8 的异常定义，finalization 阶段不新增异常类型：

| 异常类型 | 触发条件 | 上抛内容 |
|---|---|---|
| 异常 1：规则 seed 不可用 | 遍历所有可用资料后仍无法形成非空可审计 seed | 是否放宽 seed 来源或接受占位值 |
| 异常 2：rolling 输入窗口缺失 | 无法形成 completed batch > 1 的 real 数据 | 是否允许人工切片或暂缓 G3 |
| 异常 3：页面主语新冲突 | 同一页面在冻结矩阵下出现不可消解的一级主语冲突 | 矩阵修订或页面拆分 |

**在未出现上述异常前，直接按统一任务书与统一校验清单执行。**

---

## 4. 结论

1. **前三轮与所有裁决无逻辑冲突**：12 个关键维度逐一确认一致。
2. **PG17 事实与文档口径完全对齐**：13 个事实项无偏差。
3. **最终口径已收口到唯一解释空间**：7 个关键术语无歧义。
4. **三项裁决回写链路完整**：无"冻结但未落地"的情况。
5. **本轮无需新增人类裁决问题。**
