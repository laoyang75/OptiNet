# 最终冻结基线草案

状态：finalization Claude 独立输出  
更新时间：2026-04-06  
输入范围：`round1_plan/merged/*`、`round2_detail/merged/*`、`round3_execution/merged/*`、`*/decisions/*`、`01_inputs/*`、`PG17 MCP execute_sql`（2026-04-06）。  
页面规则：未重新访问页面；页面证据仅引用 round3 已归档截图与结论。

---

## 1. 总判断

前三轮 merged 结果与全部裁决（D-001 / D-002 / D-003）在语义上已经收口到唯一解释空间，不存在方向级逻辑冲突。

具体而言：

1. **三轮方向一致**：round1 提出"先合同、再 real 数据、再 API、最后页面"，round2 细化为可执行合同与 DDL 设计，round3 进一步写成逐工作包的执行任务书——三轮方向链条无断裂。
2. **裁决已全量回写**：D-001（从 l0 重跑）→ round2 §1.1.2 + round3 §0.2 + WP3；D-002（全新前端）→ round3 §6.7 + WP6；D-003（ODS 定义/执行分离）→ round2 §6.1 + round3 矩阵 governance 行。
3. **round2 / round3 无新增裁决**：两轮 `decisions/01_待裁决问题清单.md` 均确认"本轮无需新增人类裁决"。

本草案的工作不是重新评估方向，而是逐项确认冻结基线的每一个数据库事实、合同条目和 Gate 条件是否仍然成立。

---

## 2. PG17 事实基线复核（2026-04-06）

### 2.1 schema 与主链

| 事实项 | PG17 结果 | 与 round3 任务书一致性 |
|---|---:|---|
| `rebuild4` / `rebuild4_meta` schema | 不存在（0） | 一致：必须从新 schema 启动 |
| `rebuild2.l0_gps` | 38,433,729 | 一致：real init 输入 1 |
| `rebuild2.l0_lac` | 43,771,306 | 一致：real init 输入 2 |
| `rebuild3.fact_standardized` | 43,771,306 | 一致：rebuild3 当前覆盖 l0_lac 口径 |
| 四分流合计 | 43,771,306 | 一致：守恒成立（24,855,605 + 10,590,738 + 3,825,562 + 4,499,401 = 43,771,306） |
| `rebuild3.obj_cell / obj_bs / obj_lac` | 573,561 / 193,036 / 50,153 | 一致：对象层结构可参考，结果不继承 |
| `rebuild3_meta.run` | 4（full_init=1, scenario=3） | 一致：无 real rolling |
| `rebuild3_meta.batch` | 142 | 一致 |
| `rebuild3_meta.batch_snapshot` | 1,562 | 一致 |
| `rebuild3_meta.baseline_version` | 4 | 一致：登记历史存在 |
| `baseline_cell / bs / lac` 实体版本 | 各仅 `BASELINE-FULL-V1` | 一致：只有登记历史，无闭合实体历史链 |

### 2.2 元数据与治理

| 事实项 | PG17 结果 | 与 round3 任务书一致性 |
|---|---:|---|
| `rebuild2_meta.field_audit` | 27（keep=17, parse=3, drop=7） | 一致：不再沿用旧文档 19/3/5 |
| `rebuild2_meta.target_field` | 55 | 一致：不再写"约 60" |
| active `ods_clean_rule` | 26 | 一致 |
| `ods_clean_result` distinct rule_code | 24 | 一致 |
| ODS 差集 | `NULL_WIFI_MAC_INVALID`、`NULL_WIFI_NAME_INVALID` | 一致：必须显式可见 |
| `rebuild2_meta.parse_rule` | 0 | 一致：正式承接缺口 |
| `rebuild2_meta.compliance_rule` | 0 | 一致：正式承接缺口 |
| `rebuild3_meta.compare_result` | 0 | 一致：compare 无结果底座 |
| `rebuild3_meta.asset_*` 四表 | 全部 0 | 一致：governance 注册表必须重建 |

### 2.3 来源与可解释性

| 事实项 | PG17 结果 | 与 round3 任务书一致性 |
|---|---:|---|
| `data_origin` / `origin_detail` / `subject_scope` 列命中数 | 0 | 一致：rebuild3 无来源合同字段 |
| `rebuild3.fact_standardized.sample_scope_tag` 非空 | 0 | 一致：无可替代来源标签 |
| trusted 过滤：total / match / filtered / pct | 43,771,306 / 30,082,381 / 13,688,925 / 31.27% | 一致 |

### 2.4 复核结论

**全部 PG17 事实与 round3 统一最终执行任务书 §1.1 完全一致，无数据漂移。**

---

## 3. 三轮 merged 与裁决的逻辑一致性检查

### 3.1 方向级一致性

| 合同维度 | round1 冻结 | round2 冻结 | round3 冻结 | 是否一致 |
|---|---|---|---|---|
| 数据起点 | rebuild2/3 只读参考，rebuild4 自产 | D-001 已裁：从 l0 重跑 | WP3 冻结双输入标准化 | 一致 |
| 来源合同 | real/synthetic/fallback 必须是合同字段 | API 顶层 envelope 冻结 | 矩阵逐页绑定来源规则 | 一致 |
| run_type | — | 冻结三值（full_init/rolling/scenario） | 沿用 | 一致 |
| batch_snapshot | — | 冻结 4 stage + 11 metric | Gate G2/G3 校验 | 一致 |
| baseline 历史 | 允许单版本，禁止伪装上一版 | 按版本追加 + current view | G6 诚实校验 | 一致 |
| governance | 纳入正式范围 | 9 组能力冻结 | G5 + WP7 交付 | 一致 |
| compare | 降级为辅助验证 | synthetic/fallback 语义区分 | G7 降级验收 | 一致 |
| 前端方案 | — | D-002 已裁：全新项目 + 审计复制 | WP6 冻结 Vue3+TS+Vite | 一致 |
| ODS 差集 | 定义与执行不能混同 | D-003 已裁：两端点分离 | governance 矩阵行确认 | 一致 |
| parse/compliance | 不能继续只靠 Markdown | 正式落点 rebuild4_meta | WP2 seed + G1 非空校验 | 一致 |
| legacy 字段 | — | 不进入主状态，只走 legacy_ref | WP6/WP7 + P-E07 约束 | 一致 |
| trusted 损耗 | 必须进入正式范围 | 预聚合读模型冻结 | WP2/WP3 trusted_loss_* | 一致 |

### 3.2 Gate 链路一致性

| Gate | round2 定义 | round3 通过条件 | 校验清单对应项 | 是否一致 |
|---|---|---|---|---|
| G0 | 编码前合同冻结 | 03_final/ 存在 + contract_version | C-G0-01 ~ C-G0-04 | 一致 |
| G1 | schema + 元数据 | schema 已建 + 快照对齐 + 规则非空 | C-G1-01 ~ C-G1-05 | 一致 |
| G2 | real init | 1 run + 1 batch + 守恒 + baseline v1 | C-G2-01 ~ C-G2-07 | 一致 |
| G3 | real rolling | completed batch > 1 + 11 metric | C-G3-01 ~ C-G3-05 | 一致 |
| G4 | API 来源合同 | 顶层 envelope + subject_scope 对齐 | C-G4-01 ~ C-G4-04 | 一致 |
| G5 | governance | 全 endpoint 可用 + trusted loss | C-G5-01 ~ C-G5-03 | 一致 |
| G6 | baseline 诚实 | 历史可追溯 + previous_available | C-G6-01 ~ C-G6-03 | 一致 |
| G7 | compare 降级 | banner + fallback 空 envelope | C-G7-01 ~ C-G7-03 | 一致 |

### 3.3 页面矩阵一致性

round2 矩阵（§5）与 round3 矩阵（§3.2）在以下维度逐行核对：

- 12 个页面的主语、主 API、读模型、允许来源、最小数据、空状态规则——**完全一致**。
- round3 未新增页面、未删减页面、未变更主语。

### 3.4 排除范围一致性

1. launcher / 运维健康检查：round1 §4.1 第 5 条"launcher 不纳入数据合同" → round2 矩阵脚注 → round3 §2.2 第 1 条。**一致**。
2. `rebuild3_sample_meta` 等兼容资产：round1 §1.2 第 3 条 → round2 §3.5 legacy 合同 → round3 §2.2 第 2 条。**一致**。
3. compare 不回写主链：round1 §1.2 第 2 条 → round2 §6.2 → round3 §6.8。**一致**。

---

## 4. 口径唯一性检查

### 4.1 已收口到唯一解释的关键口径

| 口径 | 唯一冻结值 | 是否存在第二种解释 |
|---|---|---|
| field_audit 总数 | 27（keep=17, parse=3, drop=7） | 否：旧文档 19/3/5 已被三轮统一声明废弃 |
| target_field 总数 | 55 | 否：禁止继续写"约 60" |
| ODS 定义 / 执行 | 26 / 24，差集 2 条 | 否：D-003 已裁为两端点分离 |
| run_type 枚举 | 3 值 | 否：不预留 other |
| data_origin 枚举 | 3 值 | 否：fallback 仅用于空响应降级 |
| batch_snapshot 形状 | 4 stage + 11 metric | 否：扩展必须走合同变更 |
| full_initialization 批次语义 | 1 run + 1 logical batch | 否：checkpoint 是引擎内部 |
| baseline 历史策略 | 按版本追加 + current view | 否：不允许只保留 current |
| 前端起点 | 全新 Vue3+TS+Vite | 否：D-002 C 已裁 |
| parse/compliance 落点 | rebuild4_meta | 否：不允许继续用 Markdown/脚本 |
| trusted 损耗查询 | 预聚合读模型 | 否：不走实时大表联查 |
| legacy 字段位置 | API legacy_ref + 页面灰色参考 | 否：不进入主状态 |

### 4.2 潜在歧义排查

经逐条检查，以下领域在 round3 任务书中的表述已足够清晰，不存在"同一个词两种含义"的残留：

1. **`data_origin = fallback`**：只用于 compare 的空响应降级，且 `data = null`。主流程无 real 时走"诚实空状态"，不使用 fallback 一词。——无歧义。
2. **`subject_scope`**：仅标记 API 响应的业务主语类型，不暗示数据完整性。——无歧义。
3. **`is_current = true` 唯一约束**：适用于 `contract_version`、`rule_set_version`、`baseline_version` 三张表，各自独立约束。——无歧义。
4. **governance vs compare 的 "正式完成" 边界**：governance 计入，compare 不计入。round3 §7 DoD 第 7 条已明确。——无歧义。

---

## 5. 裁决回写完整性检查

| 裁决 | 回写到 round2 | 回写到 round3 | 校验清单对应 | 完整性 |
|---|---|---|---|---|
| D-001（从 l0 重跑） | §1.1.2 双输入重跑 + §4.1 real init 合同 | WP3 全部执行动作 | C-G2-01 ~ C-G2-07 | 完整 |
| D-002（全新前端） | 未直接写入 round2（因裁决发生在 round1 后） | §6.7 + WP6 | P00 ~ P11 | 完整 |
| D-003（ODS 分离） | §6.1 governance 9 组能力第 7/8 项 | 矩阵 governance 行 + WP7 | C-G5-02 | 完整 |

**三项裁决的回写链路完整，未发现任何裁决被"冻结但未落地"的情况。**

---

## 6. 已冻结但仍需执行验证的残留缺口

以下 3 个条目不是方向冲突，而是"冻结了合同但尚未产出实际结果"的正常执行缺口：

| 缺口 | 当前状态 | 需要什么 | 对应 Gate |
|---|---|---|---|
| `rebuild4` / `rebuild4_meta` schema 不存在 | PG17 确认 0 | Phase 1 DDL 执行 | G1 |
| `parse_rule` / `compliance_rule` = 0 | PG17 确认 0 | WP2 seed 脚本执行 | G1 |
| 无 real rolling run | PG17 确认 rebuild3 只有 scenario_replay | WP4 rolling 执行 | G3 |

这些是预期中的缺口，不影响冻结基线的成立性。它们将在正式实施阶段通过 Gate 逐步关闭。

---

## 7. 冻结基线结论

1. **前三轮 merged 与所有裁决无逻辑冲突。**
2. **最终口径已收口到唯一解释空间，不存在"同一个词多种含义"的残留。**
3. **PG17 事实基线与 round3 统一任务书完全一致，无数据漂移。**
4. **Gate G0-G7 的前置条件、通过条件、停机动作与校验清单逐项对齐。**
5. **三项裁决（D-001/D-002/D-003）的回写链路完整。**
6. **建议进入最终冻结合并，无需新增人类裁决。**
