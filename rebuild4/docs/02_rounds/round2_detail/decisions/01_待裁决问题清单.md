# 待裁决问题清单

状态：round2 已完成  
更新时间：2026-04-06  
结论：**本轮无需新增人类裁决文件**

---

## 裁决结论

经对三家 agent 候选裁决问题（`outputs/claude/03_候选裁决问题.md`、`outputs/codex/03_候选裁决问题.md`、`outputs/gemini/03_候选裁决问题.md`）与 round2 合并稿（`merged/01_统一细化设计.md`、`merged/02_统一细化校验基线.md`）逐项比对，所有候选问题已在 round2 合并稿内冻结，无需另行人类拍板。

---

## 候选问题收口情况

| 候选问题 | 来源 | 合并稿收口依据 |
|---|---|---|
| D2-001：`run_type` 枚举是否允许扩展 | Claude | 合并稿 1.1.4：冻结三值，不预留 `other`，修改必须先改合同 |
| D2-002：`full_initialization` 是否允许多 batch | Claude | 合并稿 1.1.5：对外合同冻结"1 个 real run + 1 个 logical batch" |
| D2-003：`batch_snapshot` stage/metric 枚举是否冻结 | Claude | 合并稿 1.1.6：冻结 4 stage + 11 metric；新增必须走合同变更 |
| D2-004：`data_origin` 在 API 中的位置 | Claude | 合并稿 1.1.3 + 3.2：固定为 API 顶层 envelope，不嵌套进 `data` |
| D2-005：governance 页需要多少 API 端点 | Claude | 合并稿 5（矩阵）+ 6.1：列出 9 组能力，按模块独立 endpoint |
| D2-006：画像页旧分类字段展示方式 | Claude | 合并稿 1.1.13 + 3.5：API 以 `legacy_ref` 嵌套，页面灰色参考列 |
| D2-007：空状态 UI 组件统一 | Claude | 纯 UI 风格问题，不影响方向/结构/合同/Gate，不上抛 |
| D-201：baseline 历史存储模式 | Codex | 合并稿 1.1.7：按 `baseline_version` 追加存储 + current view |
| D-202：governance 注册表填充方式 | Codex | 合并稿 1.1.10：数据库 introspection 建基数 + 人工补充语义 |
| D-203：parse/compliance 正式承接位置 | Codex | 合并稿 1.1.9：正式承接固定在 `rebuild4_meta` |
| D-204：anomaly summary 是否拆粒度 | Codex | 合并稿 1.1.12：拆成三张表（object/record/impact） |
| D-205：compare 是否允许 synthetic 结果 | Codex | 合并稿 6.2：允许，但必须顶层标注 + 页面 banner，不计正式完成 |
| D-004：parse/compliance 填充动作与执行方式 | Gemini | 合并稿 3.4：seed 脚本从文档/SQL/Python 提取，排除纯人工补录，方向已定 |
| D-005：trusted 1300 万数据查询策略 | Gemini | 合并稿 1.1.11 + 4.4：预聚合读模型（`trusted_loss_summary` + `trusted_loss_breakdown`） |

---

## 不上抛的理由

本轮收口遵循以下标准：

1. **文字风格差异不上抛**：三家 agent 在措辞、格式上有差异，但方向一致
2. **已被合并稿冻结的不上抛**：凡合并稿已给出唯一结论的，不再重复裁决
3. **纯 UI 实施层面不上抛**：空状态组件等属于 Phase 4 交付细节

---

## round3 入场说明

依据合并稿 §7，以下条件全部满足后即可进入 round3 任务书收束：

- [x] `merged/01_统一细化设计.md` + `merged/02_统一细化校验基线.md` 已接受，语义无歧义
- [ ] `rebuild4` / `rebuild4_meta` 建表清单已映射成可执行 DDL 任务（round3 执行）
- [ ] parse/compliance seed 路径已落实为迁移脚本（round3 执行）
- [ ] governance endpoint 清单和 compare 降级边界已写入最终任务书骨架（round3 执行）
- [ ] Gate G0-G7 的 SQL/API 校验项已可直接执行（round3 执行）
