# inputs

这里只放输入资料，不放当前轮输出。

使用顺序：

1. `01_foundation/`
2. `02_research/`
3. `03_ui_v2/`
4. `04_reference/`

原则：

- `foundation` 和 `research` 是当前整理后的主输入
- `ui_v2` 是页面语义基线
- `reference` 是 rebuild2 / rebuild3 的副本资料
