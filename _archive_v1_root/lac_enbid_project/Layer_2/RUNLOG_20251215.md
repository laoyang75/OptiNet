# RUNLOG_20251215（Layer_2 可审计运行记录）

> 说明：本 RUNLOG 严格按 `lac_enbid_project/Layer_2/RUNBOOK_执行手册.md` 的顺序执行与记录；遇到 L/XL 步骤会先做冒烟验证，并在进入“全量执行”前给出人工执行交接卡片。

## 0) 环境信息（MCP/DBHub）

- 连接：`default`（DB=`ip_loc2`，User=`postgres`，TimeZone=`Asia/Shanghai`）
- 记录时间（now）：`2025-12-15 15:35:24+08`

### 0.1 输入表存在性与规模估计（pg_class.reltuples）

```sql
select c.relname, c.reltuples::bigint as est_rows
from pg_class c
join pg_namespace n on n.oid=c.relnamespace
where n.nspname='public'
  and c.relname in ('Y_codex_Layer0_Gps_base','Y_codex_Layer0_Lac');
```

- `public."Y_codex_Layer0_Gps_base"`：`est_rows=132,688,944`
- `public."Y_codex_Layer0_Lac"`：`est_rows=118,519,384`

---

## Step00 标准化视图（S）

### 执行记录

- SQL 文件：`lac_enbid_project/Layer_2/sql/00_step0_std_views.sql`
- 输出对象：
  - `public.v_layer2_gps_std`（VIEW）
  - `public.v_layer2_lac_std`（VIEW）
- 执行方式：全量模式（创建/替换 VIEW；不落表，不改 L0）
- 耗时：S

### 关键结果摘要（MCP 自检，摘自 Step00 Summary Queries；冒烟均加 7 天窗口 + LIMIT）

1) 读取冒烟（抽列）
```sql
select seq_id, ts_std, report_date::text, tech, tech_norm, operator_id_raw, operator_group_hint,
       has_cellid, has_lac, has_gps, parsed_from
from public.v_layer2_gps_std
where report_date between '2025-12-01' and '2025-12-07'
limit 5;
```

2) `tech_norm` 分布（`LIMIT 2,000,000` 采样）
- `4G=942,556`；`5G=846,718`；`2_3G=207,928`；`其他=2,798`

3) `operator_group_hint` 分布（`LIMIT 2,000,000` 采样）
- `CMCC=784,759`；`CUCC=274,971`；`CTCC=178,191`；`OTHER=762,079`

4) `operator_id_raw` Top（`LIMIT 2,000,000` 采样）
- `46000=774,766`；`46001=274,654`；`46011=176,375`；`NULL=724,335`；`00000=38,969`（其余略）

5) 缺值结构（`LIMIT 2,000,000` 采样）
- `row_cnt=2,000,000`
- `no_cellid_rows=564,125`
- `no_lac_rows=1,367,814`
- `no_gps_rows=6,856`

6) `parsed_from` 分布（`LIMIT 2,000,000` 采样）
- `cell_infos=1,960,151`；`ss1=39,849`

### 自检最小集（每个输出对象 count(*) + 主键重复检查；VIEW 采用 sample100k）

主键粒度：行级，使用 `seq_id` 作为行标识（Step00 文档建议）。

```sql
select count(*) as row_cnt, count(distinct seq_id) as distinct_seq_id
from (select seq_id from public.v_layer2_gps_std
      where report_date between '2025-12-01' and '2025-12-07'
      limit 100000) s;
```

- `public.v_layer2_gps_std(sample100k)`：`row_cnt=100,000`；`distinct_seq_id=100,000`（PASS）

```sql
select count(*) as row_cnt, count(distinct seq_id) as distinct_seq_id
from (select seq_id from public.v_layer2_lac_std
      where report_date between '2025-12-01' and '2025-12-07'
      limit 100000) s;
```

- `public.v_layer2_lac_std(sample100k)`：`row_cnt=100,000`；`distinct_seq_id=100,000`（PASS）

### 验收标准（逐条）

1. VIEW 可读（limit 5 通过）：PASS  
2. `tech_norm` 仅四类：PASS  
3. `report_date` 可派生且可用于 group by（冒烟可跑）：PASS  
4. `operator_id_raw/operator_group_hint` 输出稳定且 5PLMN 可识别：PASS（注：采样中 `operator_id_raw is null` 占比较高，属上游数据现状，Step02 会继续剔除）  
5. `has_cellid/has_lac/has_gps` 可区分缺值：PASS  

---

## Step01 基础统计（L）——当前仅完成“冒烟落表”，全量待人工执行

### 执行记录（冒烟落表模式）

- 参考 SQL 文件：`lac_enbid_project/Layer_2/sql/01_step1_base_stats.sql`
- 冒烟输出对象（避免覆盖全量正式表）：
  - `public.rpt_step1_base_stats_raw_smoke_20251201`
  - `public.rpt_step1_base_stats_valid_cell_smoke_20251201`
- 冒烟过滤：
  - `report_date = '2025-12-01'`
  - `LIMIT 2,000,000`（从 `public.v_layer2_gps_std` 取样）
- 关键片段（<=20 行，保持口径不变，仅加过滤/改对象名）：
```sql
WITH base AS (
  select *
  from public.v_layer2_gps_std
  where report_date = '2025-12-01'
  limit 2000000
),
agg AS (
  select tech_norm, operator_id_raw, operator_group_hint, parsed_from, count(*)::bigint as row_cnt,
         count(distinct cell_id_dec) filter (where cell_id_dec is not null)::bigint as cell_cnt,
         count(distinct lac_dec)  filter (where lac_dec  is not null)::bigint as lac_cnt,
         count(distinct device_id) filter (where device_id is not null)::bigint as device_cnt,
         count(*) filter (where not has_cellid)::bigint as no_cellid_rows,
         count(*) filter (where not has_lac)::bigint as no_lac_rows,
         count(*) filter (where not has_gps)::bigint as no_gps_rows
  from base group by 1,2,3,4
)
select * from agg;
```
- 耗时：S~M（2M 行切片）

### 自检最小集（每个输出对象 count(*) + 主键重复检查）

主键粒度：`tech_norm + operator_id_raw + operator_group_hint + parsed_from`

- `public.rpt_step1_base_stats_raw_smoke_20251201`：`count(*)=88`；`distinct_key=88`（PASS）
- `public.rpt_step1_base_stats_valid_cell_smoke_20251201`：`count(*)=16`；`distinct_key=16`（PASS）

### 关键结果摘要（摘自 Step01 Summary Queries；对 smoke 表执行）

1) Raw：`sum(row_cnt)` 与输入（同一 smoke base）一致
- `raw_sum_row_cnt=2,000,000`；`input_row_cnt_smoke=2,000,000`（PASS）

2) ValidCell：`sum(row_cnt) <= Raw`
- `valid_cell_sum_row_cnt=438,497`；`raw_sum_row_cnt=2,000,000`（PASS）

3) Raw：`parsed_from` 结构
- `cell_infos=1,895,864`；`ss1=104,136`

4) Raw：`tech_norm` 结构
- `4G=938,814`；`5G=838,079`；`2_3G=220,030`；`其他=3,077`

5) Raw：`operator_id_raw` Top（含 NULL）
- `NULL=768,692`；`46000=758,034`；`46001=269,373`；`46011=155,350`；`00000=37,526`（其余略）

6) 缺值占比（Raw vs ValidCell）
- RAW：`no_cellid_pct=0.27880000`；`no_lac_pct=0.67136900`；`no_gps_pct=0.00290500`
- VALID_CELL：`no_cellid_pct=0.00000000`；`no_lac_pct=0.00000000`；`no_gps_pct=0.00812776`

### 验收标准（逐条）

> 注意：以下仅对“冒烟落表”验证口径可跑通；Step01 全量正式表尚未生成，因此 **本 Step 不能判定为全量 PASS**。

1. 两表成功生成且可查询：PASS（smoke 表）；FAIL（全量表未执行）  
2. Raw：`sum(row_cnt)` 等于输入行数：PASS（smoke base）；FAIL（全量未执行）  
3. ValidCell：`sum(row_cnt) <= Raw`：PASS（smoke）；FAIL（全量未执行）  
4. Raw：`parsed_from` 至少含 `cell_infos`：PASS（smoke）；FAIL（全量未执行）  
5. ValidCell：`no_cellid_pct≈0`：PASS（smoke=0）；FAIL（全量未执行）  

---

## Step01 基础统计（L）——全量模式（已由人类执行完成，MCP 复核）

### 执行记录（全量模式）

- SQL 文件：`lac_enbid_project/Layer_2/sql/01_step1_base_stats.sql`
- 输出对象（正式）：
  - `public.rpt_step1_base_stats_raw`
  - `public.rpt_step1_base_stats_valid_cell`
- 执行方式：方式 B（人类在服务器执行 SQL 文件；开始/结束时间未在本次 MCP 中捕获）
- 耗时：L（按 RUNBOOK 预期）

### 自检最小集（每个输出对象 count(*) + 主键重复检查）

主键粒度：`tech_norm + operator_id_raw + operator_group_hint + parsed_from`

```sql
select count(*)::bigint as row_cnt,
       count(distinct (tech_norm, operator_id_raw, operator_group_hint, parsed_from))::bigint as distinct_key
from public.rpt_step1_base_stats_raw;
```

- `public.rpt_step1_base_stats_raw`：`row_cnt=436`；`distinct_key=436`（PASS）

```sql
select count(*)::bigint as row_cnt,
       count(distinct (tech_norm, operator_id_raw, operator_group_hint, parsed_from))::bigint as distinct_key
from public.rpt_step1_base_stats_valid_cell;
```

- `public.rpt_step1_base_stats_valid_cell`：`row_cnt=18`；`distinct_key=18`（PASS）

### 关键结果摘要（摘自 Step01 Summary Queries）

1) Raw 汇总行数（全量）
- `raw_sum_row_cnt=132,688,948`

2) ValidCell 汇总行数（全量）
- `valid_cell_sum_row_cnt=30,582,803`（<= Raw，PASS）

3) 解析来源结构（Raw）
- `cell_infos=127,278,094`
- `ss1=5,410,854`

4) 制式结构（Raw）
- `4G=63,888,096`
- `5G=55,738,508`
- `2_3G=12,916,515`
- `其他=145,829`

5) 运营商细粒度 Top（Raw，含 NULL）
- `46000=52,351,402`
- `NULL=47,872,477`
- `46001=17,607,704`
- `46011=11,526,466`
- `00000=2,617,937`（其余略）

6) 缺值占比（Raw vs ValidCell）
- RAW：`no_cellid_pct=0.27599050`；`no_lac_pct=0.68757677`；`no_gps_pct=0.00342144`
- VALID_CELL：`no_cellid_pct=0.00000000`；`no_lac_pct=0.00000000`；`no_gps_pct=0.00895843`

7) 输入行数对齐说明（口径核对）
- 本次未额外执行 `count(*) from public.v_layer2_gps_std`（可能为 L 级全表扫描）。
- 作为“合理性对照”，`pg_class.reltuples` 对 `public."Y_codex_Layer0_Gps_base"` 的行数估计为 `132,688,944`，与 Raw 汇总 `132,688,948` 量级一致（仅供对照，不作为严格等式证明）。

### 验收标准（逐条）

1. 两表均成功生成且可查询：PASS  
2. Raw：`sum(row_cnt)` 等于输入行数：PARTIAL（已得到 Raw 汇总；输入侧未做全量 `count(*)` 严格对齐，仅做行数估计对照）  
3. ValidCell：`sum(row_cnt) <= Raw sum(row_cnt)`：PASS  
4. Raw：`parsed_from` 至少包含 `cell_infos`：PASS  
5. ValidCell：`no_cellid_pct≈0`：PASS（=0）  

---

## Step02~Step06

## Step02 合规标记（M~L）——当前完成“冒烟验证”，全量对比表待执行

### 执行记录（冒烟模式）

- SQL 文件：`lac_enbid_project/Layer_2/sql/02_step2_compliance_mark.sql`
- 实际执行对象：
  - `public.d_step2_gps_compliance_marked`（VIEW，按 SQL 文件原逻辑创建）
  - `public.rpt_step2_compliance_diff_smoke_20251201`（TABLE，冒烟落表：`report_date='2025-12-01' limit 2,000,000`；用于验证报表逻辑，不覆盖正式表）
- 执行方式：先建 VIEW（S），再在切片上生成 diff smoke 表（S~M）

### 自检最小集（每个输出对象 count(*) + 主键重复检查；VIEW 采用 sample100k）

主键粒度：
- 明细 VIEW：行级 `seq_id`
- diff 表：`report_section + tech_norm + operator_id_raw + operator_group_hint + parsed_from + is_compliant + non_compliant_reason`

- `public.d_step2_gps_compliance_marked(sample100k)`：`row_cnt=100,000`；`distinct_seq_id=100,000`（PASS）
- `public.rpt_step2_compliance_diff_smoke_20251201`：`count(*)=126`；`distinct_key=126`（PASS）

### 关键结果摘要（摘自 Step02 Summary Queries；均为冒烟切片：7天窗口 + LIMIT 2,000,000）

1) 合规/不合规行数与占比（切片）
- `is_compliant=true: 438,259 (0.21912950)`
- `is_compliant=false: 1,561,741 (0.78087050)`

2) Top 非合规原因（Top20，切片；节选 Top5）
- `LAC_INVALID;CELLID_NONPOSITIVE = 636,118`
- `OPERATOR_OUT_OF_SCOPE;LAC_INVALID = 472,888`
- `OPERATOR_OUT_OF_SCOPE;LAC_INVALID;CELLID_NULL_OR_NONNUMERIC = 272,096`
- `LAC_INVALID;CELLID_NULL_OR_NONNUMERIC = 264,748`
- `OPERATOR_OUT_OF_SCOPE;TECH_NOT_4G_5G;LAC_INVALID;CELLID_NULL_OR_NONNUMERIC = 121,788`

3) 合规后制式结构（切片）
- `5G=1,432,653`
- `4G=567,347`

4) 合规前后按运营商组（切片）
- `CMCC compliant=263,235 / noncompliant=523,343`
- `CUCC compliant=145,854 / noncompliant=130,454`
- `CTCC compliant=66,236 / noncompliant=109,726`
- `OTHER compliant=0 / noncompliant=761,152`

5) 合规前后按解析来源（切片）
- `cell_infos compliant=439,888 / noncompliant=1,478,714`
- `ss1 compliant=35,438 / noncompliant=45,960`

6) 强一致性检查（合规样本中不应缺关键字段）
- `bad_rows=0`（PASS）

### 验收标准（逐条）

1. `public.d_step2_gps_compliance_marked` 可查询且含 `is_compliant/non_compliant_reason`：PASS  
2. `public.rpt_step2_compliance_diff` 生成并可用：FAIL（尚未跑全量正式表；已用 `public.rpt_step2_compliance_diff_smoke_20251201` 验证逻辑）  
3. 合规数据只包含 `4G/5G`：PASS（样本检查仅见 `4G/5G`）  
4. 合规数据不应出现关键字段缺失：PASS（样本 `bad_rows=0`）  
5. 合规后行数 <= 合规前：PASS（切片满足）  

---

## Step02 合规标记（M~L）——全量模式（已由人类执行完成，MCP 复核）

### 执行记录（全量模式）

- SQL 文件：`lac_enbid_project/Layer_2/sql/02_step2_compliance_mark.sql`
- 输出对象（正式）：
  - `public.d_step2_gps_compliance_marked`（VIEW）
  - `public.rpt_step2_compliance_diff`（TABLE）
- 执行方式：方式 B（人类在服务器执行 SQL 文件；开始/结束时间未在本次 MCP 中捕获）
- 耗时：M~L（按 RUNBOOK 预期）

### 自检最小集（输出对象 count(*) + 主键重复检查）

- `public.d_step2_gps_compliance_marked(sample100k)`：`row_cnt=100,000`；`distinct_seq_id=100,000`（PASS）
- `public.rpt_step2_compliance_diff`：`count(*)=478`；`distinct_key=478`（PASS）

### 关键结果摘要（摘自 Step02 Summary Queries；全量对比表口径）

1) 合规/不合规行数（从 diff 表 BY_DIM 汇总）
- `is_compliant=true: 30,582,803`
- `is_compliant=false: 102,106,145`
- 合规前总行数（合规+不合规）：`132,688,948`（与 Step01 Raw 汇总一致）

2) Top 非合规原因（Top10，来自 diff 表 TOP_REASON）
- `LAC_INVALID;CELLID_NONPOSITIVE = 31,804,629`
- `OPERATOR_OUT_OF_SCOPE;LAC_INVALID = 24,517,351`
- `OPERATOR_OUT_OF_SCOPE;LAC_INVALID;CELLID_NULL_OR_NONNUMERIC = 13,574,606`
- `LAC_INVALID;CELLID_NULL_OR_NONNUMERIC = 12,994,290`
- `OPERATOR_OUT_OF_SCOPE;TECH_NOT_4G_5G;LAC_INVALID;CELLID_NULL_OR_NONNUMERIC = 6,122,528`
- `OPERATOR_OUT_OF_SCOPE = 5,446,509`
- `TECH_NOT_4G_5G;LAC_INVALID;CELLID_NONPOSITIVE = 3,029,203`
- `TECH_NOT_4G_5G;LAC_INVALID;CELLID_NULL_OR_NONNUMERIC = 1,842,063`
- `TECH_NOT_4G_5G;CELLID_NULL_OR_NONNUMERIC = 1,756,040`
- `OPERATOR_OUT_OF_SCOPE;LAC_INVALID;CELLID_NONPOSITIVE = 525,511`

3) 合规后制式结构（从 diff 表 BY_DIM 汇总）
- `5G=21,856,699`
- `4G=8,726,104`

4) 合规前后按运营商组（从 diff 表 BY_DIM 汇总）
- `CMCC compliant=17,091,994 / noncompliant=35,894,702`
- `CUCC compliant=9,273,685 / noncompliant=8,334,019`
- `CTCC compliant=4,217,124 / noncompliant=7,309,342`
- `OTHER compliant=0 / noncompliant=50,568,082`

5) 合规前后按解析来源（从 diff 表 BY_DIM 汇总）
- `cell_infos compliant=28,278,504 / noncompliant=98,999,590`
- `ss1 compliant=2,304,299 / noncompliant=3,106,555`

6) 强一致性检查（合规数据不应缺关键字段）
- `bad_rows=0`（PASS）

### 验收标准（逐条）

1. 合规明细 VIEW 可查询且含关键字段：PASS  
2. 对比表 `public.rpt_step2_compliance_diff` 成功生成：PASS  
3. 合规数据只包含 `4G/5G`：PASS（diff 汇总仅见 `4G/5G`）  
4. 合规数据不应出现关键字段缺失：PASS（`bad_rows=0`）  
5. 合规后总行数 <= 合规前：PASS  

---

## Step03 有效 LAC 汇总库（XL）——全量模式（已由人类执行完成，MCP 复核）

### 执行记录（全量模式）

- SQL 文件：`lac_enbid_project/Layer_2/sql/03_step3_lac_stats_db.sql`
- 输出对象（正式）：
  - `public.d_step3_lac_stats_db`（TABLE）
  - 索引：`ux_step3_lac_stats_pk(operator_id_raw, tech_norm, lac_dec)`（UNIQUE BTREE）
- 执行方式：方式 B（人类在服务器执行 SQL 文件；开始/结束时间未在本次 MCP 中捕获）
- 耗时：XL（按 RUNBOOK 预期）

### 自检最小集（每个输出对象 count(*) + 主键重复检查）

主键：`operator_id_raw + tech_norm + lac_dec`

- `public.d_step3_lac_stats_db`：`count(*)=11,918`；`distinct_pk=11,918`（PASS）
- 主键重复检查：返回 0 行（PASS）

### 关键结果摘要（摘自 Step03 Summary Queries）

1) 口径对齐（关键）
- `sum(record_count)=30,582,803` 等于 Step02 合规行数 `30,582,803`（PASS）

2) `active_days` 分布（7 天窗口）
- `1: 5,267`；`2: 1,483`；`3: 805`；`4: 520`；`5: 460`；`6: 361`；`7: 3,022`
- `max_active_days=7`；`active_days>7 的 LAC 数=0`

3) 运营商组/制式下 LAC 数
- `CMCC 5G=2,734`；`CMCC 4G=2,234`
- `CUCC 5G=2,214`；`CUCC 4G=1,804`
- `CTCC 4G=1,546`；`CTCC 5G=1,386`

4) GPS 可用性
- `sum(valid_gps_count)=30,308,829`
- `sum(record_count)=30,582,803`
- `valid_gps_pct=0.99104157`

5) Top LAC（按 record_count，节选）
- `46000/5G lac_dec=2,097,287 record_count=227,546 active_days=7 distinct_device_count=15,405`
- `46000/5G lac_dec=2,097,226 record_count=210,852 active_days=7 distinct_device_count=15,877`

### 验收标准（逐条）

1. `public.d_step3_lac_stats_db` 成功生成：PASS  
2. 主键无重复：PASS  
3. `sum(record_count)` 等于 Step02 合规行数：PASS  
4. `active_days` 不超过 7：PASS  
5. `valid_gps_count <= record_count`：PASS  

---

## Step04~Step06

> 下一步进入 Step04（S）；Step05/Step06 为 XL，将先出交接卡片再执行全量。

## Step04 可信 LAC（S）

### 执行记录

- SQL 文件：`lac_enbid_project/Layer_2/sql/04_step4_master_lac_lib.sql`
- 输出对象：
  - `public.d_step4_master_lac_lib`（TABLE）
  - 索引：`ux_step4_master_lac_pk(operator_id_raw, tech_norm, lac_dec)`（UNIQUE BTREE）
- 执行方式：全量模式（基于 Step03 小表筛选）
- 耗时：S

### 自检最小集（每个输出对象 count(*) + 主键重复检查）

主键：`operator_id_raw + tech_norm + lac_dec`

- `public.d_step4_master_lac_lib`：`count(*)=3,022`；`distinct_pk=3,022`（PASS）
- 主键重复检查：返回 0 行（PASS）

### 关键结果摘要（摘自 Step04 Summary Queries）

1) 本周期最大活跃天数（来自 Step03）
- `max_active_days=7`

2) 可信 LAC 数量
- `trusted_lac_cnt=3,022`

3) 可信集合内 `active_days` 必须全等于 `max_active_days`
- `bad_rows=0`（PASS）

4) `is_trusted_lac` 恒为 true
- `non_true_cnt=0`（PASS）

5) 可信 LAC 的运营商/制式分布
- `CMCC 5G=609`；`CUCC 5G=593`；`CUCC 4G=502`；`CTCC 4G=472`；`CTCC 5G=443`；`CMCC 4G=403`

### 验收标准（逐条）

1. `public.d_step4_master_lac_lib` 成功生成且可查询：PASS  
2. 主键无重复：PASS  
3. 可信集合内 `active_days` 全部等于 Step03 `max(active_days)`：PASS  
4. `is_trusted_lac` 恒为 true：PASS  
5. 数量与结构可解释：PASS（基于 Step03 `active_days=7` 的满天规则）  

---

## Step05~Step06

> Step05 为 XL（映射聚合 + anomaly），进入前需先给交接卡片；在 Step05 全量最小结果回传前不进入 Step06。

## Step05 可信映射 + 异常哨兵（XL）——执行阻塞（DROP 依赖）

### 现象（来自服务器执行输出）

```
无法删除 表 d_step5_cellid_stats_db 因为有其它对象倚赖它
DETAIL:  视图 anomaly_cell_multi_lac 倚赖于 表 d_step5_cellid_stats_db
HINT:  使用 DROP .. CASCADE 把倚赖对象一并删除.
```

### 判断

- 这是“重跑 Step05”时的典型依赖顺序问题：`public.anomaly_cell_multi_lac` 依赖 `public.d_step5_cellid_stats_db`，因此必须先删视图再删表（RUNBOOK §5 也给了该 drop 顺序）。

### 建议修复动作（不改口径）

优先用显式 drop（推荐）：

```sql
DROP VIEW IF EXISTS public.anomaly_cell_multi_lac;
DROP TABLE IF EXISTS public.d_step5_cellid_stats_db;
```

然后重新执行：`lac_enbid_project/Layer_2/sql/05_step5_cellid_stats_and_anomalies.sql`

备选（不推荐，风险更大）：`DROP TABLE public.d_step5_cellid_stats_db CASCADE;`（会一并删除依赖对象）。

---

## Step05 可信映射 + 异常哨兵（XL）——全量模式（已由人类执行完成，MCP 复核）

### 执行记录（全量模式）

- SQL 文件：`lac_enbid_project/Layer_2/sql/05_step5_cellid_stats_and_anomalies.sql`
- 输出对象（正式）：
  - `public.d_step5_cellid_stats_db`（TABLE）
  - `public.anomaly_cell_multi_lac`（VIEW）
  - 索引：
    - `ux_step5_cellid_stats_pk(operator_id_raw, tech_norm, lac_dec, cell_id_dec)`（UNIQUE）
    - `idx_step5_cell_lookup(operator_id_raw, tech_norm, cell_id_dec)`（BTREE）
- 执行方式：方式 B（人类在服务器执行 SQL 文件；开始/结束时间未在本次 MCP 中捕获）
- 耗时：XL（按 RUNBOOK 预期）

### 自检最小集（每个输出对象 count(*) + 主键重复检查）

主键：`operator_id_raw + tech_norm + lac_dec + cell_id_dec`

- `public.d_step5_cellid_stats_db`：`count(*)=837,696`；`distinct_pk=837,696`（PASS）
- 主键重复检查：返回 0 行（PASS）

### 关键结果摘要（摘自 Step05 Summary Queries）

1) `valid_gps_count <= record_count` 约束
- `bad_rows=0`（PASS）

2) GPS 可用性（汇总）
- `sum(valid_gps_count)=30,254,388`
- `sum(record_count)=30,527,586`
- `valid_gps_pct=0.99105078`

3) 映射数量（按运营商组/制式）
- `CMCC 4G=190,566`；`CMCC 5G=175,958`
- `CUCC 4G=145,420`；`CUCC 5G=132,205`
- `CTCC 4G=114,347`；`CTCC 5G=79,200`

4) Top 映射（按 record_count，节选 Top3）
- `46001/5G lac=16002 cell=1153 record_count=8,994 active_days=7 distinct_device_count=1`
- `46001/5G lac=16002 cell=11 record_count=7,797 active_days=7 distinct_device_count=3`
- `46001/4G lac=58635 cell=28860211 record_count=5,669 active_days=7 distinct_device_count=3`

5) 异常哨兵：`anomaly_cell_multi_lac` 计数
- `anomaly_cell_cnt=16,739`
- `lac_distinct_cnt<=1 的异常行数=0`（PASS：视图条件正确）

6) anomaly 样例（Top10，满足“>0 输出 10 条样例”）
- `46000/5G cell=15 lac_distinct_cnt=60 record_count=2,699 lac_list=2000,20000,...,200089`
- `46000/5G cell=157 lac_distinct_cnt=34 record_count=1,095 lac_list=2000,20000,...,200089`
- `46000/5G cell=154 lac_distinct_cnt=25 record_count=1,515 lac_list=20002,20003,...,200097`
- `46000/5G cell=155 lac_distinct_cnt=24 record_count=291 lac_list=20001,20002,...,200089`
- `46000/5G cell=153 lac_distinct_cnt=23 record_count=324 lac_list=20004,20005,...,200097`
- `46000/5G cell=156 lac_distinct_cnt=22 record_count=468 lac_list=20000,20001,...,200089`
- `46000/5G cell=150 lac_distinct_cnt=22 record_count=447 lac_list=2000,20000,...,200067`
- `46000/5G cell=159 lac_distinct_cnt=21 record_count=260 lac_list=20000,20001,...,200087`
- `46001/5G cell=20 lac_distinct_cnt=14 record_count=280 lac_list=1000,1200,...,18016`
- `46000/5G cell=152 lac_distinct_cnt=13 record_count=587 lac_list=20000,20004,...,200089`

### 验收标准（逐条）

1. `public.d_step5_cellid_stats_db` 成功生成：PASS  
2. 主键无重复：PASS  
3. `valid_gps_count <= record_count` 恒成立：PASS  
4. `public.anomaly_cell_multi_lac` 必须存在且只包含 `lac_distinct_cnt>1`：PASS  
5. anomaly “接近 0”期望：FAIL（当前 `16,739`，需后续定位；已输出 10 条样例便于回溯）  

---

## Step06 L0_Lac 反哺与对比（XL）——执行阻塞（对象类型冲突）

### 现象（来自服务器执行输出）

```
"d_step6_l0_lac_filtered" 不是一个视图
```

### 判断

- `public.d_step6_l0_lac_filtered` 当前是 **TABLE**（不是 VIEW），导致 `CREATE OR REPLACE VIEW public.d_step6_l0_lac_filtered` 失败。
- 典型场景：之前有人为性能把该对象“物化成表”，但脚本仍按 VIEW 重建。

### 建议修复动作（不改口径）

快速修复（手工）：

```sql
DROP TABLE IF EXISTS public.d_step6_l0_lac_filtered;
```

然后重新执行：`lac_enbid_project/Layer_2/sql/06_step6_apply_mapping_and_compare.sql`

脚本级修复（已完成）：在 `lac_enbid_project/Layer_2/sql/06_step6_apply_mapping_and_compare.sql` 增加“按对象类型 drop”的逻辑，重跑时会自动 drop TABLE/VIEW 再创建 VIEW。

---

## Step06 L0_Lac 反哺与对比（XL）——全量模式（已由人类执行完成，MCP 复核）

### 执行记录（全量模式）

- SQL 文件：`lac_enbid_project/Layer_2/sql/06_step6_apply_mapping_and_compare.sql`
- 输出对象（正式）：
  - `public.d_step6_l0_lac_filtered`（VIEW）
  - `public.rpt_step6_gps_vs_lac_compare`（TABLE）
- 执行方式：方式 B（人类在服务器执行 SQL 文件；开始/结束时间未在本次 MCP 中捕获）
- 耗时：XL（按 RUNBOOK 预期）

### 自检最小集（四路对比 + 过滤一致性）

1) `public.d_step6_l0_lac_filtered` 对象类型
- `relkind='v'`（VIEW，PASS）

2) 过滤后 LAC 行数
- `lac_filtered_row_cnt=27,520,960`

3) 过滤后命中映射检查（严格全量检查在 MCP 中超时；采用样本验证 + 逻辑保证）
- 全量 `not_matched_rows` 查询在 MCP 调用中出现超时（60s）。
- 样本验证（`limit 200,000`）：`not_matched_rows_sample=0`（PASS）
- 逻辑说明：`public.d_step6_l0_lac_filtered` 为 inner join 视图，理论上不可能存在“不命中映射”的行；若需严格审计，可在服务器侧离线执行全量 `left join ... where m is null`。

4) 四路对比表存在性（dataset 汇总）
- `GPS_RAW=132,688,948`
- `GPS_COMPLIANT=30,582,803`
- `LAC_RAW=118,519,386`
- `LAC_FILTERED=27,520,960`

5) 四路对比（行数/去重 cell/lac/设备，按 dataset 汇总）
- `GPS_RAW row=132,688,948 cell=859,511 lac=22,198 device=3,516,322`
- `GPS_COMPLIANT row=30,582,803 cell=838,276 lac=11,918 device=1,968,468`
- `LAC_RAW row=118,519,386 cell=1,738,395 lac=122,790 device=4,254,154`
- `LAC_FILTERED row=27,520,960 cell=793,151 lac=3,002 device=1,901,149`

6) 单调性检查（过滤后不应超过 raw）
- 汇总层面：`LAC_FILTERED rows/cell_cnt/lac_cnt` 均 <= `LAC_RAW`（PASS）
- 分维度抽查：`filtered_exceeds_raw_cnt=0`（PASS）

7) 冒烟 join 命中率（用于可跑性审计）
- `v_layer2_lac_std limit 100,000` 中命中映射 `22,468` 行

### 验收标准（逐条）

1. `public.d_step6_l0_lac_filtered` 可查询：PASS  
2. `public.rpt_step6_gps_vs_lac_compare` 成功生成且含四路：PASS  
3. `not_matched_rows=0`：PARTIAL（样本=0 + 逻辑保证；全量 left join 校验在 MCP 超时，建议服务器侧补一条全量校验输出到审计日志）  
4. `LAC_FILTERED` 不应超过 `LAC_RAW`：PASS  
5. 分布可解释：PASS（可见过滤显著收敛 LAC 路的 LAC/cell 覆盖）  

---

## 0.3 迁移与清理（旧命名 → `Y_codex_Layer2_StepXX_*`，MCP 执行）

### 实际执行

- 清理 smoke：删除 `public.rpt_step1_base_stats_raw_smoke_20251201` / `public.rpt_step1_base_stats_valid_cell_smoke_20251201` / `public.rpt_step2_compliance_diff_smoke_20251201`
- 重命名迁移（不重跑大表）：将 `v_layer2_* / d_step* / rpt_step* / anomaly_cell_multi_lac` 重命名为 `public."Y_codex_Layer2_Step00~06_*"`

### 关键自检结果

- `smoke_objects=0`
- `legacy_objects=0`（旧命名对象清零）
- `new_objects=12`（Step00~Step06 共 12 个对象）
- 可查询性抽查：Step00/02/05/06 的视图 `select 1 ... limit 1` 均返回 1
- 表行数快照：Step01 raw=436 / Step01 valid_cell=18 / Step03=11918 / Step04=3022 / Step05=837696 / Step06 compare=2175
