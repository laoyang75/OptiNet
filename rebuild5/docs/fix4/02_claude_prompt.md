# Claude Prompt

你在 `/Users/yangcongan/cursor/WangYou_Data` 中独立承担 `Fix4` 的一条完整研究线。

你必须完全独立完成自己的分析、实现和验证。

注意：独立研究不代表原始样例数据可以不同。你必须使用同一份共享样例数据集。

## 先读这些文件

- `rebuild5/docs/fix4/00_fix4_任务说明.md`
- `rebuild5/docs/fix4/03_shared_dataset_contract.md`
- `rebuild5/docs/fix4/04_shared_dataset_spec.md`
- `rebuild5/docs/fix3/04_数据链路完整审计报告.md`
- `rebuild5/docs/fix3/多质心问题分析.md`
- `rebuild5/docs/fix3/postgis_multicentroid_batch7_report.md`
- `rebuild5/docs/03_流式质量评估.md`
- `rebuild5/docs/05_画像维护.md`

## 任务拆解

### Part A：先解决逻辑问题

当前最优先问题不是性能，而是位置计算质量。

请围绕这些问题展开：

1. 为什么当前质心计算无法有效过滤无效 GPS / 偏差最大点？
2. 这些异常点应在哪一层被排除：
   - Step3
   - Step5
   - 两层协同
3. 目前 Cell 覆盖面积为什么会明显偏大？
4. 这种失真如何继续传导到 BS？
5. `p80 / p90 / 其他稳健半径口径` 哪种更适合当前主链？

你必须优先修逻辑，再谈性能。

### Part B：逻辑正确后再优化速度

速度优化目标是：

- 不牺牲位置质量
- 样例数据完整跑通 7 批
- 给出修复前后速度变化

## 样例数据要求

你必须使用共享样例数据集。

你的独立性体现在：

- 方案设计
- 代码实现
- 参数选择
- 结果解释

而不是体现在：

- 自己换原始输入
- 自己改样例成员

如果你认为样例不够，请输出补样建议，不要直接修改原始样例集。

你必须直接使用这三张共享表：

- `rebuild5_fix4.raw_gps_shared_sample`
- `rebuild5_fix4.etl_cleaned_shared_sample`
- `rebuild5_fix4.focus_cells_shared`

这三张表已经建好并有数据，不需要你自行导入。

固定时间范围就是：

- `2025-12-01 ~ 2025-12-07`

这里要特别说明：

- “共享样例固定”只约束原始输入
- 你仍然可以在共享原始表之上自由构建自己的派生工作集
- 这部分自由度是被允许且鼓励的，但必须可复现

## 必须验证的内容

你必须实际完成：

- 基于共享原始样例表构建派生工作集
- 完整 `7` 次 ETL / 日批次运行
- 面积变化验证
- 速度变化验证

不是只做离线 SQL 对比，也不是只做部分批次。

## 业务边界

请严格遵守：

- `LAC` 不做多簇
- `BS` 作为聚合层，继承 Cell 分类结果，不直接照搬 Cell 原始点聚类逻辑
- `Cell` 的目标分类重点是：
  - `dual_cluster`
  - `multi_cluster`
  - `moving`
  - `migration`

其中：

- `migration` 只应建立在双簇基础上，再结合时间关系判断
- `moving` 是可被 BS 继承的 Cell 标签
- `large_coverage` 需要分析，但不是当前第一优先级的在线分类目标

## 输出目录

把你的结果放到：

- `rebuild5/docs/fix4/claude/`

至少包括：

1. `report.md`
2. `runbook.md`
3. `dataset_plan.md`
4. `metrics_summary.json`

## 速度与质量基线

当前参考基线：

- `batch3 Step5 ≈ 124s`
- `batch4 Step5 ≈ 344s`
- `batch5 Step5 ≈ 689s`

当前没有统一黄金标签的绝对面积阈值。

所以你必须在共享样例数据上做：

- before / after 面积对比
- before / after 速度对比
- 并解释为什么面积缩小是合理的，而不是简单裁剪

## report.md 必须包含

1. 当前逻辑问题是什么
2. 你如何修复它
3. 为什么修复后能解决覆盖面积过大
4. 修复后面积变化
5. 修复后速度变化
6. 还剩哪些问题没解决

## runbook.md 必须包含

1. 共享样例数据如何使用，以及具体用哪张共享表
   - 明确 7 批固定日期就是 `2025-12-01 ~ 2025-12-07`
   - 明确你在共享原始样例表之上构建了哪些派生工作集
2. 如何执行 7 批 ETL / 日增量
3. 如何验证每批结果
4. 如果中断如何恢复

## 你要特别注意

- 不要直接跑正式全库
- 不要只给概念性建议
- 不要把“推测”写成“已验证”

## 执行前要求

开始动手前，必须先把“先读这些文件”章节里的文档读完。

这不是可选项。

原因：

- 当前任务强依赖 Step3 / Step5 的现有职责边界
- 如果不了解这些前置文档，后续很容易误解批处理/流式处理的边界

你的输出应该能让我把这条研究线的结果与另一条独立研究线做横向比较，再决定最终采用哪版修复方案。
