# Codex Prompt

你在 `/Users/yangcongan/cursor/WangYou_Data` 中独立执行一轮 `Fix4` 研究与实现。

你的任务不是给建议，而是：

1. 基于真实代码和真实数据库访问能力，独立分析当前位置计算与覆盖面积问题。
2. 先修逻辑问题，再做速度优化。
3. 用共享样例数据完整跑通 `7` 次 ETL / 日增量链路。
4. 形成可交付的修复代码、runbook 和研究报告。

## 你必须先读

- `rebuild5/docs/fix4/00_fix4_任务说明.md`
- `rebuild5/docs/fix4/03_shared_dataset_contract.md`
- `rebuild5/docs/fix4/04_shared_dataset_spec.md`
- `rebuild5/docs/fix3/04_数据链路完整审计报告.md`
- `rebuild5/docs/fix3/多质心问题分析.md`
- `rebuild5/docs/fix3/postgis_multicentroid_batch7_report.md`
- `rebuild5/docs/03_流式质量评估.md`
- `rebuild5/docs/05_画像维护.md`

## 你的主目标

### 第一阶段：先解决逻辑问题

重点处理：

1. Step3 / Step5 位置计算对异常点不敏感，导致无效 GPS 进入质心计算。
2. 当前策略无法有效过滤偏差最大的点。
3. Cell 覆盖面积过大。
4. BS 覆盖面积被 Cell 主质心失真放大。
5. 多质心与迁移/移动的边界不够稳定。

你需要重点判断：

- 异常点应该在 Step3 先过滤，还是只在 Step5 处理，还是两层都要做。
- 如果按 `p80` 而不是 `p90` 计算主服务半径，是否更适合作为当前主链默认口径。
- 哪些过滤应在质心计算前完成，哪些应作为异常标签后置。

### 第二阶段：在逻辑稳定后做速度优化

优化重点只能建立在逻辑正确之后。

不能为了速度直接牺牲位置质量。

## 数据要求

你必须使用**共享样例数据集**。

这意味着：

1. 你可以基于共享样例数据构建中间表
2. 你可以提出补样建议
3. 你可以设计自己的分析视图

但你不能：

1. 私自增删原始样例 Cell
2. 私自更换时间范围
3. 私自改用另一套原始输入

这里要特别说明：

- “共享样例固定”只约束原始输入
- 你仍然可以在共享原始表之上自由构建自己的派生工作集
- 这部分自由度是被允许且鼓励的，但必须可复现

你必须直接使用这三张共享表：

- `rebuild5_fix4.raw_gps_shared_sample`
- `rebuild5_fix4.etl_cleaned_shared_sample`
- `rebuild5_fix4.focus_cells_shared`

这三张表已经建好并有数据，不需要你自行导入。

固定时间范围就是：

- `2025-12-01 ~ 2025-12-07`

## 运行要求

你必须实际完成完整样例验证，而不是纸上设计：

- 跑完整 `7` 次 ETL / 日批次
- 明确每一批的结果变化
- 关注速度变化和面积变化

注意：

- 这里的 7 批时间范围固定就是 `2025-12-01 ~ 2025-12-07`
- 这不是可选项，也不是自行模拟的 7 次循环

## 业务边界

- LAC 不做多簇。
- BS 不能直接复用 Cell 的原始点多簇逻辑，但应继承 Cell 的稳定分类结果。
- Cell 需要重点区分：
  - `dual_cluster`
  - `multi_cluster`
  - `moving`
  - `migration`
- `migration` 只应该是双簇基础上的时间关系再判断，不要泛化到任意多簇。
- `large_coverage` 不是本轮第一优先级分类目标，但要给出是否保留、如何延后研究的意见。

## 交付物

把你的所有结果输出到：

- `rebuild5/docs/fix4/codex/`

至少包括：

1. `report.md`
   - 逻辑问题诊断
   - 修复方案
   - 为什么能解决覆盖面积过大
   - 风险点
2. `runbook.md`
   - 明确使用哪张共享样例表
   - 明确 7 批固定日期就是 `2025-12-01 ~ 2025-12-07`
   - 如何跑 7 次 ETL / 日批次
   - 如何验证结果
3. `dataset_plan.md`
   - 明确共享原始样例表如何使用
   - 明确你构建了哪些派生工作集
   - 每个派生工作集对应的用途和 SQL / 脚本
   - 如果样例不够，列出补样建议，但不要私自改样例集
4. `metrics_summary.json`
  - 修复前后面积变化
  - 修复前后速度变化
  - 每批关键统计

## 速度与质量基线

当前参考基线：

- `batch3 Step5 ≈ 124s`
- `batch4 Step5 ≈ 344s`
- `batch5 Step5 ≈ 689s`

当前没有统一黄金标签的绝对面积阈值。

所以你必须在共享样例数据上做：

- before / after 面积对比
- before / after 速度对比
- 并解释为什么面积缩小是合理的，而不是简单裁剪

## 输出风格要求

- 不要只给“建议”，要给实际代码改动和实际验证结果。
- 明确区分：
  - 已验证事实
  - 高置信推断
  - 待进一步验证项
- 如果你选择某个策略，例如 `p80`，必须说明为什么。

## 执行前要求

开始动手前，必须先把“你必须先读”章节里的文档读完。

这不是可选项。

原因：

- 当前任务强依赖 Step3 / Step5 的现有职责边界
- 如果不了解这些前置文档，后续很容易误解批处理/流式处理的边界

## 最终目标

最终你要交付的是：

- 一版基于样例数据可跑通的修复方案
- 一版可执行的 runbook
- 一版“修复后如何解决覆盖面积问题”的说明
- 一版“速度变化 / 面积变化”的量化研究

不要跑正式全库，不要把结果直接覆盖正式数据。
