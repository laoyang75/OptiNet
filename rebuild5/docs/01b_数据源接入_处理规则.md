# Step 1: 数据源接入 — 处理规则

本文冻结 `rebuild5` Step 1 的具体处理规则。所有规则必须自包含，不能依赖“详见其他项目文档”的隐式知识。

## 解析规则

### 处理链路

```text
原始报文（27 列）
    |
    +-- 读取 cell_infos JSON
    |      -> 每个主服务小区 1 行
    |
    +-- 读取 ss1 文本
    |      -> 每个信号组 1 行
    |
    +-- 保留 record_id / cell_id / 时间 / GPS / 元数据
    |
    +-- 合并为 etl_parsed
```

## `cell_infos` 解析

### JSON 输入格式（完整示例）

```json
{
  "1": {
    "isConnected": 1,
    "type": "nr",
    "cell_identity": {
      "mccString": "460",
      "mncString": "01",
      "Nci": 563347713,
      "Tac": 65546,
      "Pci": 515,
      "ChannelNumber": 633984
    },
    "signal_strength": {
      "SsRsrp": -88,
      "SsRsrq": -11,
      "SsSinr": 8,
      "Dbm": -88,
      "AsuLevel": 52,
      "Level": 2
    }
  }
}
```

### 全部提取字段表

#### 结构化字段

| 目标字段 | JSON 路径 | 说明 |
|----------|-----------|------|
| `tech_norm` | `cell.type` | 制式标准化：`lte -> 4G`，`nr -> 5G`，`gsm -> 2G`，`wcdma -> 3G` |
| `operator_code` | `cell.cell_identity.mno` 或 `mccString + mncString` | 运营商编码 |
| `lac` | `Tac / tac / lac / Lac` | 位置区编码 |
| `cell_id` | `Ci / Nci / nci / cid` | 小区标识 |
| `pci` | `Pci` | 物理小区标识 |
| `freq_channel` | `Earfcn / ChannelNumber / arfcn / uarfcn` | 频点号 |
| `bandwidth` | `Bwth` | 带宽（kHz） |
| `rsrp` | `signal_strength.rsrp / SsRsrp` | 参考信号接收功率 |
| `rsrq` | `signal_strength.rsrq / SsRsrq` | 参考信号接收质量 |
| `sinr` | `signal_strength.rssnr / SsSinr` | 信噪比 |
| `rssi` | `signal_strength.rssi` | 接收信号强度 |
| `dbm` | `signal_strength.Dbm` | 分贝毫瓦 |
| `asu_level` | `signal_strength.AsuLevel` | ASU 级别 |
| `sig_level` | `signal_strength.Level` | SDK 信号等级 |
| `timing_advance` | `signal_strength.TimingAdvance` | 时间提前量 |
| `csi_rsrp` | `signal_strength.CsiRsrp` | 5G CSI 信号 |
| `csi_rsrq` | `signal_strength.CsiRsrq` | 5G CSI 信号 |
| `csi_sinr` | `signal_strength.CsiSinr` | 5G CSI 信号 |
| `cqi` | `signal_strength.cqi` | 信道质量 |
| `lon_raw`, `lat_raw` | 原始字段 `原始上报gps` | 报文级原始 GPS 经纬度 |

#### 直传字段清单

| 字段 | 原始来源 | 说明 |
|------|----------|------|
| `record_id` | `记录数唯一标识` | 原始记录唯一标识 |
| `data_source_detail` | `数据来源dna或daa` | 数据源说明 |
| `dev_id` | `did` | 设备标识 |
| `ip` | `ip` | 上报 IP |
| `plmn_main` | `主卡运营商id` | 主卡运营商 |
| `brand` | `品牌` | 手机品牌 |
| `model` | `机型` | 手机机型 |
| `sdk_ver` | `sdk_ver` | SDK 版本 |
| `oaid` | `oaid` | 匿名设备标识 |
| `pkg_name` | `pkg_name` | 应用包名 |
| `wifi_name` | `wifi_name` | WiFi SSID |
| `wifi_mac` | `wifi_mac` | WiFi BSSID |
| `cpu_info` | `cpu_info` | CPU 信息 |
| `pressure` | `压力` | 气压读数 |
| `ts_raw` | `ts` | 上报时间原文 |
| `gps_ts_raw` | `gps上报时间` | GPS 上报时间原文 |
| `gps_info_type` | `gps_info_type` | GPS 来源类型 |

### 处理规则

| 规则 | 说明 |
|------|------|
| `jsonb_each()` | 把 `cell_infos` JSON 的每个 key/value 展开成独立候选行 |
| 只保留 `isConnected=1` | 只保留主服务基站 |
| `cell_id` 不能为空 | 解析阶段就排除无法取出 `cell_id` 的 JSON 节点 |
| `gps_filled_from='raw_gps'` | `cell_infos` 行的原始 GPS 来源标记 |
| `gps_valid` 初值 | `gps_info_type in ('gps','1')` 记为 `true`，其余为 `false` |

## `ss1` 解析

### 文本输入格式

`ss1` 是分号分组、每组 4 段、段内再按逗号切分的文本串：

```text
信号段&时间段&GPS段&基站段;信号段&时间段&GPS段&基站段;...
```

### 每段说明

| 段 | 格式 | 示例 | 说明 |
|----|------|------|------|
| 信号段 | 逗号分隔，首字母=制式 | `l,-85,-10,15` | 一组信号值 |
| 时间段 | unix 秒 | `1765011706` | 本组扫描时间 |
| GPS 段 | `经度,纬度` 或空 | `116.397,39.908` | 本组自带 GPS |
| 基站段 | `制式,cell_id,lac,plmn` 或 `1` | `l,12345,4176,46000` | 本组基站信息或继承标记 |

### 基站继承（forward-fill）逻辑

```text
组1: ... & l,12345,4176,46000    <- 本组自带基站
组2: ... & 1                      <- 继承组1基站
组3: ... & n,67890,73733,46001   <- 新基站
组4: ... & 1                      <- 继承组3基站
```

规则：
- `cell_block='1'` 表示继承本 `record_id` 内前一组最近一次出现的有效基站段。
- 如果当前组没有可继承基站，则该组不能生成结构化行。
- 同组内若存在多个候选基站或信号条目，可继续按 `+` 拆分，再按制式匹配。

### `ss1` 解析规则

| 规则 | 说明 |
|------|------|
| 先按 `;` 拆组 | 一条 `ss1` 可拆成多个组 |
| 再按 `&` 拆 4 段 | `sig_block`、`ts_block`、`gps_block`、`cell_block` |
| `cell_block` 支持 forward-fill | `1` 表示继承前一组基站 |
| `sig_block` / `cell_block` 可再按 `+` 拆分 | 用于处理同组多条候选 |
| 信号与基站按制式匹配 | `l` 对 `4G`，`n` 对 `5G`，`g` 对 `2G` |
| `gps_filled_from='ss1_own'` | 本组自带 GPS 的行保留该来源标记 |

### `ss1` 特有字段

| 字段 | 说明 |
|------|------|
| `sig_ss` | `ss1` 原始信号值（信号段第 2 个值） |
| `cell_ts_raw` | 组级时间戳（unix 秒） |

### `ss1` 没有的字段（置 NULL）

```text
pci, freq_channel, bandwidth,
rssi, dbm, asu_level, sig_level,
timing_advance,
csi_rsrp, csi_rsrq, csi_sinr, cqi
```

## 解析统计

### 样本数据统计

| 指标 | 值 |
|------|----|
| 原始记录数 | `249,201` |
| `cell_infos`（GPS 样本） | `201,518` |
| `cell_infos`（LAC 样本） | `188,337` |
| `ss1`（GPS 样本） | `149,771` |
| `ss1`（LAC 样本） | `149,735` |
| 合并总行数 | `689,361` |
| 扩展比 | `2.77x` |

### 字段覆盖率

| 字段 | 覆盖率 | 说明 |
|------|--------|------|
| `operator_code` | `~99.8%` | 极少数缺失 |
| `lac` | `~99.9%` | 解析覆盖较高 |
| `cell_id` | `~99.8%` | 少量异常值 |
| `rsrp` | `~88%` | `ss1` 有时无信号匹配 |
| `lon_raw/lat_raw` | `~85%` | `ss1` 部分组无 GPS |
| `freq_channel` | `~57%` | 仅 `cell_infos` 有 |
| `rssi` | `~16%` | 仅部分 LTE 有 |
| `wifi_name` | `~46%` | 原始数据部分有 |
| `pressure` | `~12%` | 仅部分设备有气压传感器 |

## 清洗规则

### 规则总览

Step 1 固定使用 `ODS-001 ~ ODS-018` 共 19 条规则，分 6 类：

```text
运营商（2 条）
LAC（4 条）
CellID（3 条）
信号（4 条）
位置/GPS（4 条）
WiFi（2 条）
```

### 类别概览表

| 类别 | 规则编号 | 数量 | 动作类型 |
|------|----------|------|----------|
| 运营商 | `ODS-001 ~ ODS-002` | 2 | 置空 |
| LAC | `ODS-003 ~ ODS-005` | 4 | 置空 |
| CellID | `ODS-006 ~ ODS-008` | 3 | 置空 + 最终删行 |
| 信号 | `ODS-009 ~ ODS-012` | 4 | 置空 |
| 位置/GPS | `ODS-013 ~ ODS-016` | 4 | 标记无效 / 经纬度置空 |
| WiFi | `ODS-017 ~ ODS-018` | 2 | 置空 |

### 运营商清洗（`ODS-001 ~ ODS-002`）

| ID | 规则 | 条件 | 动作 |
|----|------|------|------|
| `ODS-001` | 垃圾编码 | `'00000'`, `'0'`, `'000000'`, `'(null)(null)'`, `''` | `operator_code` 置空 |
| `ODS-002` | 非白名单 | 不在白名单中的 `operator_code` | `operator_code` 置空 |

#### 运营商白名单完整列表

| `operator_code` | 中文 |
|-----------------|------|
| `46000` | 移动 |
| `46001` | 联通 |
| `46002` | 移动 |
| `46003` | 电信 |
| `46005` | 电信 |
| `46006` | 联通 |
| `46007` | 移动 |
| `46009` | 联通 |
| `46011` | 电信 |
| `46015` | 广电 |
| `46020` | 铁路 |

### LAC 清洗（`ODS-003 ~ ODS-005`）

| ID | 规则 | 条件 | 动作 | 说明 |
|----|------|------|------|------|
| `ODS-003` | `LAC=0` | `lac = 0` | `lac` 置空 | 未获取到有效 LAC |
| `ODS-004` | 4G 保留值 | `lac IN (65534,65535) AND tech_norm='4G'` | `lac` 置空 | 16bit 保留值 0xFFFE/0xFFFF |
| `ODS-004b` | 5G 保留值 | `lac IN (16777214,16777215) AND tech_norm='5G'` | `lac` 置空 | 24bit 保留值 0xFFFFFE/0xFFFFFF |
| `ODS-005` | 溢出值 | `lac IN (268435455, 2147483647)` | `lac` 置空 | 28bit 0xFFFFFFF / INT_MAX 溢出值 |

### CellID 清洗（`ODS-006 ~ ODS-008`）

| ID | 规则 | 条件 | 动作 | 说明 |
|----|------|------|------|------|
| `ODS-006` | `CellID=0` | `cell_id = 0` | `cell_id` 置空 | 无小区 |
| `ODS-007` | 5G 溢出 | `cell_id = 268435455 AND tech_norm='5G'` | `cell_id` 置空 | 28bit 溢出 |
| `ODS-008` | 4G 溢出 | `cell_id = 2147483647 AND tech_norm='4G'` | `cell_id` 置空 | `Integer.MAX_VALUE` |

### 信号清洗（`ODS-009 ~ ODS-012`）

说明：本节是 Step 1 的 ODS 严格边界，优先级高于全局约定中的物理可入库范围。

| ID | 字段 | 有效范围 | 动作 | 说明 |
|----|------|----------|------|------|
| `ODS-009` | `RSRP` | `-156 ~ -1` | 越界置空 | `0` 和正数都是无效值 |
| `ODS-010` | `RSRQ` | `-34 ~ 10` | 越界置空 | 使用 ODS 严格范围 |
| `ODS-011` | `SINR` | `-23 ~ 40` | 越界置空 | 使用 ODS 严格范围 |
| `ODS-012` | `Dbm` | `< 0` | 越界置空 | `0` 和正数无效 |

#### ODS 边界与全局约定的关系

| 字段 | ODS 清洗边界 | 全局约定物理范围 | 采用口径 |
|------|--------------|------------------|----------|
| `RSRP` | `-156 ~ -1` | `-156 ~ 0` | Step 1 以 ODS 为准 |
| `RSRQ` | `-34 ~ 10` | `-50 ~ 0` | Step 1 以 ODS 为准 |
| `SINR` | `-23 ~ 40` | `-30 ~ 50` | Step 1 以 ODS 为准 |

### 位置 / GPS 清洗（`ODS-013 ~ ODS-016`）

| ID | 规则 | 条件 | 动作 |
|----|------|------|------|
| `ODS-013` | 非原生 GPS 标记无效 | `gps_info_type` 不是 `'gps'` 或 `'1'` | `gps_valid=false` |
| `ODS-014` | 非原生 GPS 经纬度置空 | `gps_valid=false` 且有坐标 | `lon_raw/lat_raw` 置空 |
| `ODS-015` | 经度越界 | `lon_raw < 73` 或 `lon_raw > 135` | `gps_valid=false` |
| `ODS-016` | 纬度越界 | `lat_raw < 3` 或 `lat_raw > 54` | `gps_valid=false` |

#### `gps_info_type` 值说明

| 值 | 含义 | 是否可信 |
|----|------|----------|
| `'gps'` | 原生 GPS | 是 |
| `'1'` | 原生 GPS（数值编码） | 是 |
| `'2'` | WiFi 定位 | 否 |
| `'4'` | 基站定位 | 否 |
| `'5'` | 其他来源 | 否 |
| `NULL` | `ss1` 行无该字段；GPS 来自 `gps_block` | 单独判断 |

### WiFi 清洗（`ODS-017 ~ ODS-018`）

| ID | 规则 | 条件 | 动作 | 说明 |
|----|------|------|------|------|
| `ODS-017` | 无效 WiFi 名称 | `wifi_name IN ('<unknown ssid>', 'unknown', '')` | `wifi_name` 置空 | 占位符 SSID 无价值 |
| `ODS-018` | 无效 WiFi MAC | `wifi_mac IN ('02:00:00:00:00:00', '00:00:00:00:00:00', '')` | `wifi_mac` 置空 | 全零或随机化 MAC 无价值 |

### 最终行过滤

置空完成后，执行最终删行：

```text
删除条件（OR）：
- cell_id IS NULL
- event_time_std IS NULL
```

原因：
- 没有有效 `cell_id` 的行无法归属实体，无法作为后续画像输入。
- 没有有效事件时间的行无法参与时间相关计算（去重、跨度、窗口）。

说明：`cell_id = 0`、溢出值等已在 ODS-006~008 阶段被置空，在此处统一通过 `IS NULL` 清理。

### 派生字段

#### 计算公式

| 字段 | 公式 | 说明 |
|------|------|------|
| `bs_id` | `4G: cell_id / 256`；`5G: cell_id / 4096` | 基站 ID |
| `sector_id` | `4G: cell_id % 256`；`5G: cell_id % 4096` | 扇区 ID |
| `operator_cn` | 按 `operator_code` 映射 | 运营商中文名 |
| `report_ts` | `ts_raw` 标准时间化 | 标准上报时间 |
| `cell_ts_std` | `ss1` 的 `cell_ts_raw`（unix 秒）转标准时间 | 标准基站扫描时间 |
| `gps_ts` | `gps_ts_raw`（unix 毫秒）转标准时间 | 标准 GPS 时间 |
| `has_cell_id` | `cell_id IS NOT NULL AND cell_id != 0` | 是否有有效 CellID |

#### `operator_cn` 映射表

| `operator_code` | `operator_cn` |
|-----------------|---------------|
| `46000` / `46002` / `46007` | 移动 |
| `46001` / `46006` / `46009` | 联通 |
| `46003` / `46005` / `46011` | 电信 |
| `46015` | 广电 |
| `46020` | 铁路 |

### 清洗统计

| 指标 | 值 |
|------|----|
| 输入（`etl_parsed`） | `689,361` |
| 删除行 | `1,573` |
| 输出（`etl_cleaned`） | `687,788` |
| 通过率 | `99.77%` |

## 补齐规则（同报文字段对齐）

### 补齐范围声明

```text
只做同报文（同 record_id）内的字段互补
不做跨报文补数
不做基于 trusted Cell 库的知识补数
```

| 边界 | 是否允许 |
|------|----------|
| 同一 `record_id` + 同一 `cell_id` 互补 | 允许 |
| 不同 `record_id` 之间互补 | 禁止 |
| 基于历史可信 Cell 补 GPS / 信号 | 禁止（属于 Step 4） |

### 核心思路

同一次手机上报炸开后，同一个 `cell_id` 可能同时出现在 `cell_infos` 与 `ss1`：

```text
record_id = abc123
├── cell_infos -> cell 12345，有 GPS，有信号，有 WiFi
├── ss1 组1   -> cell 12345，有信号，可能有 GPS
└── ss1 组2   -> cell 12345，有信号，无 GPS
```

为什么可以用 `cell_id` 匹配：
- 匹配范围被限制在同一个 `record_id` 内。
- 同一次手机上报中，同一个 `cell_id` 指向同一物理基站的概率极高。
- 因此 Step 1 允许在同一报文内按 `record_id + cell_id` 做结构性互补。

### `cell_infos` 行之间的补齐

同一 `record_id` + 同一 `cell_id` 的 `cell_infos` 行，时间完全一致，允许全字段补齐。

可补齐字段：

```text
operator_code, lac,
rsrp, rsrq, sinr, dbm,
lon/lat (GPS),
wifi_name, wifi_mac
```

### `ss1` 行的时间约束

`ss1` 每组有自己的 `cell_ts_raw`。对 `ss1 <- donor` 的补齐必须先判断时间差。

| 时间差 | 允许补齐的字段 | 原因 |
|--------|----------------|------|
| `<= 1 分钟` | 全字段可补 | 1 分钟内位置和射频环境通常仍连续 |
| `> 1 分钟` | 只补 `operator_code`、`lac` | 超过 1 分钟后，GPS / 信号 / WiFi 可能已失真 |

### 完整字段补齐矩阵

| 字段 | `cell_infos <- cell_infos` | `ss1 <- cell_infos` | `ss1 <- ss1`（`<=1分钟`） | `ss1 <- ss1`（`>1分钟`） | 说明 |
|------|----------------------------|---------------------|-----------------------------|----------------------------|------|
| `operator_code` | 可补 | 可补 | 可补 | 可补 | 运营商短时间内稳定 |
| `lac` | 可补 | 可补 | 可补 | 可补 | LAC 短时间内稳定 |
| `rsrp` | 可补 | 可补 | 可补 | 不补 | 信号随位置快速变化 |
| `rsrq` | 可补 | 可补 | 可补 | 不补 | 同上 |
| `sinr` | 可补 | 可补 | 可补 | 不补 | 同上 |
| `dbm` | 可补 | 可补 | 可补 | 不补 | 同上 |
| `lon/lat` | 可补 | 可补 | 可补 | 不补 | GPS 随移动变化 |
| `wifi_name` | 可补 | 可补 | 可补 | 不补 | WiFi 环境随位置变化 |
| `wifi_mac` | 可补 | 可补 | 可补 | 不补 | 同上 |

### 补齐优先级

#### GPS 优先级

| 优先级 | 来源 | 说明 |
|--------|------|------|
| 1 | `raw_gps` | `cell_infos` 原始 GPS，优先级最高 |
| 2 | `ss1_own` | `ss1` 本组自带 GPS |
| 3 | `same_cell` | 同报文内 donor 补入 |
| 4 | `none` | 无可用 GPS |

#### 信号优先级

| 优先级 | 规则 |
|--------|------|
| 1 | 优先保留当前行已有信号 |
| 2 | 当前行为空时，从同报文同 `cell_id` donor 补 |
| 3 | 多个候选 donor 时，取最强值（`RSRP` 最大） |

#### 运营商 / LAC 优先级

| 优先级 | 规则 |
|--------|------|
| 1 | 优先保留当前行已有值 |
| 2 | 当前行为空时，从 `cell_infos` donor 补 |
| 3 | 若只有 `ss1` donor，则从最近可用 `ss1` donor 补 |

### 补齐来源标记

#### 通用来源标记

| 标记 | 含义 |
|------|------|
| `original` | 当前行原始就有值 |
| `same_cell` | 来自同报文、同 `cell_id` donor |
| `none` | 没有补到 |

#### GPS 特殊来源标记

GPS 需要区分原始来源，所以比通用标记更细：

| 标记 | 含义 |
|------|------|
| `raw_gps` | `cell_infos` 原始 GPS |
| `ss1_own` | `ss1` 本组自带 GPS |
| `same_cell` | 同报文 donor GPS |
| `none` | 没有补到 |

### 补齐统计

| 指标 | 补齐前 | 补齐后 | 提升 |
|------|--------|--------|------|
| GPS 覆盖率 | `84.8%` | `97.5%` | `+12.7%` |
| RSRP 覆盖率 | `86.9%` | `94.6%` | `+7.7%` |
| 行数 | `687,788` | `687,788` | `0` |

### 实现约束

| 约束 | 说明 |
|------|------|
| 不增减行数 | 字段对齐只填空，不新建、不删除记录 |
| 不覆盖基础真相列 | 原始 / 清洗后的基础列保留，对齐结果写 `_filled` 列 |
| 强制记录来源 | 所有关键补齐结果都必须有来源标记 |
| 不跨报文 | 匹配键固定为 `record_id + cell_id` |
| 不进入知识补数 | 可信赖库补数在 Step 4 单独实现 |
